# 《制霸新手村的骷髅怪》体力系统策划案

> 版本: 1.0.5 | 更新日期: 2026-04-18 | 状态: 待开发

---

## 一、概述

### 1.1 目的
设计体力系统，控制玩家游戏节奏，增加商业化空间（体力购买），提升游戏留存。

### 1.2 体力系统定位
- **核心机制**：限制玩家无限刷关卡，保持关卡稀缺感
- **商业化入口**：体力不足时可购买，增加变现机会
- **留存机制**：等待体力恢复增加每日登录动力

### 1.3 设计原则
1. **不卡玩家**：体力消耗合理，新手期体力充足
2. **渐进限制**：后期关卡消耗更多体力，增加挑战
3. **商业友好**：体力不足时提供购买选项

---

## 二、体力数值设计

### 2.1 基础参数

| 参数 | 数值 | 说明 |
|------|------|------|
| 体力上限 | 100 | 最大体力值 |
| 初始体力 | 100 | 新玩家起始体力 |
| 恢复时间 | 5分钟/点 | 每5分钟恢复1点体力 |
| 恢复定时 | 实时 | 离开游戏也计算（仅作参考） |

### 2.2 关卡体力消耗

| 关卡类型 | 体力消耗 | 说明 |
|----------|----------|------|
| 1-10关 | 5点/关 | 新手期低消耗 |
| 11-20关 | 8点/关 | 进阶期 |
| 21-30关 | 10点/关 | 中期 |
| 31-40关 | 12点/关 | 后期 |
| 41-50关 | 15点/关 | 高难度 |
| 51-60关 | 20点/关 | 精英/终极 |

### 2.3 体力恢复计算

```
恢复公式：每5分钟恢复1点
恢复时间 = (上限 - 当前体力) × 5分钟

示例：
- 体力100 → 消耗50 → 剩余50
- 恢复满需要：(100-50) × 5分钟 = 250分钟 ≈ 4小时
```

---

## 三、体力道具

### 3.1 体力恢复道具

| 道具ID | 名称 | 图标 | 恢复体力 | 价格 |
|--------|------|------|----------|------|
| energy_1 | 体力药水(小) | 🧪 | 30点 | 6元 |
| energy_2 | 体力药水(中) | 🧪 | 60点 | 15元 |
| energy_3 | 体力药水(大) | 🧪 | 100点 | 30元 |

### 3.2 月卡体力特权

| 月卡类型 | 每日体力 | 价格 |
|----------|----------|------|
| 月卡 | +100/天 | 30元 |
| 年卡 | +200/天 | 328元 |

---

## 四、UI设计

### 4.1 体力显示位置

**方案A：显示在主角Tab顶部**
```
┌─────────────────────────────────────┐
│  👤 主角                             │
├─────────────────────────────────────┤
│  ⚡ 体力: 85/100      [立即恢复]    │ ← 体力条
│  ████████████░░░░░░░               │
├─────────────────────────────────────┤
│  角色卡片...                         │
└─────────────────────────────────────┘
```

**方案B：显示在顶部导航栏下方**
```
┌─────────────────────────────────────┐
│  ⚡ 85/100  ████████░░  [恢复]      │
├─────────────────────────────────────┤
│  👤  🎯  ⭐  🏆  🗺️  🛒           │ ← 导航栏
└─────────────────────────────────────┘
```

**方案C：显示在关卡Tab每个关卡卡片上**
```
┌─────────────────────────────────────┐
│  🎯 关卡                             │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │ ❄️ 第一章 · 冰雪初现         │   │
│  │                    ⚡ 5点   │   │
│  │  [1] [2] [3] [4] [5] [6]   │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │ 💨 第二章 · 暴风骤起 🔒      │   │
│  │                    ⚡ 8点   │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

### 4.2 推荐方案

采用**方案C：关卡卡片显示体力消耗**

原因：
- 玩家在选择关卡时就知道消耗
- 体力不足时卡片显示锁定状态
- 不占用额外UI空间

### 4.3 体力不足弹窗

```
┌─────────────────────────────────────┐
│                                     │
│            ⚠️ 体力不足              │
│                                     │
│      当前体力: 3/100                │
│      通关需要: 5点                  │
│                                     │
│      是否消耗道具恢复体力？          │
│                                     │
│   ┌─────────┐  ┌─────────┐        │
│   │  取消   │  │  恢复   │        │
│   └─────────┘  └─────────┘        │
│                                     │
│   ┌─────────────────────────┐      │
│   │   💰 立即购买体力      →│      │
│   └─────────────────────────┘      │
│                                     │
└─────────────────────────────────────┘
```

### 4.4 体力恢复提示（商城入口）

```
┌─────────────────────────────────────┐
│  💰 体力购买                         │
├─────────────────────────────────────┤
│                                     │
│  🧪 体力药水(小)    30点    ¥6      │
│  🧪 体力药水(中)    60点    ¥15     │
│  🧪 体力药水(大)   100点   ¥30     │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  💡 提示：体力每5分钟恢复1点         │
│                                     │
└─────────────────────────────────────┘
```

---

## 五、功能流程

### 5.1 开始关卡流程

```
玩家点击关卡
    ↓
检查体力是否充足
    ↓
┌─────────────────────────────────┐
│  体力充足 (≥ 消耗)              │
├─────────────────────────────────┤
│  显示确认弹窗                    │
│  "消耗5点体力，开始挑战？"       │
│                                  │
│  [取消]  [开始]                  │
└─────────────────────────────────┘
    ↓
玩家点击"开始"
    ↓
扣除体力
    ↓
开始游戏
```

### 5.2 体力不足流程

```
玩家点击关卡
    ↓
检查体力不足 (< 消耗)
    ↓
┌─────────────────────────────────┐
│  ⚠️ 体力不足弹窗                │
│                                  │
│  当前体力: 3/100                │
│  通关需要: 5点                  │
│                                  │
│  [取消]  [用道具恢复]           │
│  [💰 立即购买体力]              │
└─────────────────────────────────┘
    ↓
玩家选择
    ↓
┌─────────────────────────────────┐
│  选择"用道具恢复"               │
│  → 检查背包是否有体力药水        │
│  → 有则使用并开始游戏           │
│  → 无则提示"没有体力药水"       │
│                                  │
│  选择"立即购买"                 │
│  → 打开商城体力购买页面          │
└─────────────────────────────────┘
```

### 5.3 体力恢复流程

```
┌─────────────────────────────────┐
│  方式1：道具恢复                │
│  ─────────────────────────────  │
│  背包使用体力药水               │
│  → 当前体力 += 道具恢复量       │
│  → 当前体力 = min(当前+恢复, 上限)│
│  → 道具数量 -1                  │
│                                  │
│  方式2：商城购买                │
│  ─────────────────────────────  │
│  打开商城 → 体力分类 → 选择商品 │
│  → 购买成功 → 体力恢复          │
│                                  │
│  方式3：时间自动恢复            │
│  ─────────────────────────────  │
│  在线期间：实时恢复             │
│  离线期间：记录上次在线时间     │
│  重新登录：计算离线时间并恢复    │
└─────────────────────────────────┘
```

### 5.4 关卡通关体力返还（可选）

```
┌─────────────────────────────────┐
│  通关奖励                        │
│  ─────────────────────────────  │
│  ✅ 通关成功：体力消耗不返还     │
│  ❌ 主动退出：体力消耗不返还     │
│  ✅ 失败继续（看广告）：同通关   │
└─────────────────────────────────┘
```

---

## 六、技术实现

### 6.1 新增变量

```javascript
// ==================== 体力系统 ====================

// 体力配置
const ENERGY_CONFIG = {
    maxEnergy: 100,           // 体力上限
    initEnergy: 100,          // 初始体力
    recoverTime: 5 * 60 * 1000, // 恢复时间（5分钟/点，毫秒）
    recoverAmount: 1          // 每次恢复量
};

// 玩家体力状态
let playerEnergy = 100;       // 当前体力
let lastEnergyUpdate = Date.now(); // 上次体力更新时间

// 关卡体力消耗表（按关卡区间）
const ENERGY_COST = {
    1: 5,    // 1-10关
    11: 8,   // 11-20关
    21: 10,  // 21-30关
    31: 12,  // 31-40关
    41: 15,  // 41-50关
    51: 20   // 51-60关
};

// 体力道具
const ENERGY_ITEMS = [
    { id: 'energy_1', name: '体力药水(小)', icon: '🧪', amount: 30, price: 6 },
    { id: 'energy_2', name: '体力药水(中)', icon: '🧪', amount: 60, price: 15 },
    { id: 'energy_3', name: '体力药水(大)', icon: '🧪', amount: 100, price: 30 }
];

// 背包中的体力道具数量
let energyItemCount = {
    'energy_1': 0,
    'energy_2': 0,
    'energy_3': 0
};

// 体力不足弹窗
let energyModal = {
    show: false,
    targetStage: 1
};
```

### 6.2 新增函数

| 函数名 | 功能 |
|--------|------|
| getEnergyCost(stageId) | 获取关卡体力消耗 |
| consumeEnergy(amount) | 消耗体力 |
| recoverEnergy(amount) | 恢复体力 |
| canStartStage(stageId) | 检查是否能开始关卡 |
| updateEnergyRealtime() | 实时更新体力（游戏中调用） |
| calculateOfflineEnergy() | 计算离线恢复体力 |
| drawEnergyBar() | 绘制体力条（主角Tab） |
| drawEnergyCost() | 绘制关卡体力消耗 |
| openEnergyModal() | 打开体力不足弹窗 |
| useEnergyItem() | 使用体力道具 |
| handleEnergyModalClick() | 处理体力弹窗点击 |

### 6.3 获取关卡体力消耗

```javascript
function getEnergyCost(stageId) {
    if (stageId >= 51) return ENERGY_COST[51];
    if (stageId >= 41) return ENERGY_COST[41];
    if (stageId >= 31) return ENERGY_COST[31];
    if (stageId >= 21) return ENERGY_COST[21];
    if (stageId >= 11) return ENERGY_COST[11];
    return ENERGY_COST[1];
}
```

### 6.4 检查能否开始关卡

```javascript
function canStartStage(stageId) {
    const cost = getEnergyCost(stageId);
    return playerEnergy >= cost;
}
```

### 6.5 实时体力更新

```javascript
function updateEnergyRealtime() {
    if (playerEnergy >= ENERGY_CONFIG.maxEnergy) {
        lastEnergyUpdate = Date.now();
        return;
    }

    const now = Date.now();
    const elapsed = now - lastEnergyUpdate;
    const recoverInterval = ENERGY_CONFIG.recoverTime; // 5分钟

    if (elapsed >= recoverInterval) {
        const pointsToRecover = Math.floor(elapsed / recoverInterval);
        const actualRecover = Math.min(
            pointsToRecover,
            ENERGY_CONFIG.maxEnergy - playerEnergy
        );
        playerEnergy += actualRecover;
        lastEnergyUpdate = now - (elapsed % recoverInterval);
    }
}
```

### 6.6 计算离线恢复

```javascript
function calculateOfflineEnergy() {
    try {
        const lastTime = wx.getStorageSync('zombieHunterLastTime');
        if (lastTime) {
            const elapsed = Date.now() - lastTime;
            const maxOfflineTime = 3 * 24 * 60 * 60 * 1000; // 最多离线3天
            const effectiveElapsed = Math.min(elapsed, maxOfflineTime);

            const pointsToRecover = Math.floor(effectiveElapsed / ENERGY_CONFIG.recoverTime);
            playerEnergy = Math.min(
                playerEnergy + pointsToRecover,
                ENERGY_CONFIG.maxEnergy
            );
        }
    } catch (e) {}

    lastEnergyUpdate = Date.now();
    savePlayerData();
}
```

### 6.7 数据持久化

```javascript
function savePlayerData() {
    const data = {
        level: player.level,
        exp: player.exp,
        gold: player.gold,
        diamond: player.diamond || 0,
        kills: player.kills,
        energy: playerEnergy,
        lastEnergyUpdate: lastEnergyUpdate,
        energyItems: energyItemCount,
        stageProgress: stageProgress,
        talentData: talentData,
        highestUnlockedChapter: highestUnlockedChapter
    };
    wx.setStorageSync('zombieHunterPlayer', JSON.stringify(data));
}

function loadPlayerData() {
    try {
        const saved = wx.getStorageSync('zombieHunterPlayer');
        if (saved) {
            const data = JSON.parse(saved);
            player.level = data.level || 1;
            player.exp = data.exp || 0;
            player.gold = data.gold || 0;
            player.diamond = data.diamond || 0;
            player.kills = data.kills || 0;
            playerEnergy = data.energy || ENERGY_CONFIG.initEnergy;
            lastEnergyUpdate = data.lastEnergyUpdate || Date.now();
            energyItemCount = data.energyItems || { 'energy_1': 0, 'energy_2': 0, 'energy_3': 0 };
            // ... 其他数据加载
        }
    } catch (e) {
        // 使用默认值
    }

    // 计算离线体力
    calculateOfflineEnergy();
}
```

---

## 七、关卡Tab集成

### 7.1 关卡卡片显示体力消耗

在 `drawMainMenuLevel()` 中，每个关卡卡片显示：

```
┌─────────────┐
│    [1]      │ ← 关卡号
│     ❄️      │ ← 关卡图标
│   第1关     │ ← 关卡名
│   霜冻平原  │
│             │
│   ⚡ 5点   │ ← 体力消耗（新增）
└─────────────┘
```

### 7.2 体力不足时关卡锁定

```javascript
// 关卡卡片状态判断
const energyCost = getEnergyCost(levelNum);
const canPlay = playerEnergy >= energyCost || stageProgress[stageIdx];

if (!canPlay) {
    // 显示锁定状态
    ctx.fillText('⚠️ 体力不足', ...);
    // 卡片置灰
}
```

### 7.3 点击关卡前检查

```javascript
// 在 handleMainMenuTouch 中的关卡点击处理
const energyCost = getEnergyCost(levelNum);
if (playerEnergy < energyCost) {
    // 打开体力不足弹窗
    energyModal.show = true;
    energyModal.targetStage = levelNum;
    return;
}
```

---

## 八、商城集成

### 8.1 体力商品数据

```javascript
// 在 SHOP_ITEMS.item 中已有定义
item: [
    { id: 'energy_1', name: '体力药水(小)', icon: '🧪', desc: '恢复30点体力', price: 6 },
    { id: 'energy_2', name: '体力药水(中)', icon: '🧪', desc: '恢复60点体力', price: 15 },
    { id: 'energy_3', name: '体力药水(大)', icon: '🧪', desc: '恢复100点体力', price: 30 }
]
```

### 8.2 购买成功后恢复体力

```javascript
function onPurchaseSuccess(itemId) {
    switch (itemId) {
        case 'energy_1':
            recoverEnergy(30);
            energyItemCount['energy_1']++;
            break;
        case 'energy_2':
            recoverEnergy(60);
            energyItemCount['energy_2']++;
            break;
        case 'energy_3':
            recoverEnergy(100);
            energyItemCount['energy_3']++;
            break;
    }
    savePlayerData();
}
```

---

## 九、验收标准

### 9.1 功能验收

- [ ] 体力系统初始化正确（100点上限）
- [ ] 关卡显示体力消耗
- [ ] 体力不足时弹窗提示
- [ ] 体力充足时正常开始关卡
- [ ] 关卡开始时扣除体力
- [ ] 体力实时恢复（每5分钟1点）
- [ ] 离线体力计算正确
- [ ] 体力道具可购买
- [ ] 体力道具可使用
- [ ] 体力数据保存和加载正确

### 9.2 UI验收

- [ ] 主角Tab显示体力条
- [ ] 关卡Tab显示体力消耗
- [ ] 体力不足弹窗样式正确
- [ ] 体力恢复动画流畅

### 9.3 备案截图要求

| 截图 | 内容 |
|------|------|
| 截图1 | 主角Tab（显示体力条） |
| 截图2 | 关卡Tab（显示体力消耗） |
| 截图3 | 体力不足弹窗 |
| 截图4 | 体力商品购买页面 |

---

## 十、开发工作量估算

| 模块 | 预估时间 |
|------|---------|
| 体力系统基础变量和函数 | 1小时 |
| 关卡Tab显示体力消耗 | 30分钟 |
| 体力不足弹窗 | 30分钟 |
| 体力检查和扣除逻辑 | 30分钟 |
| 实时体力恢复 | 20分钟 |
| 离线体力计算 | 30分钟 |
| 体力道具集成（商城） | 30分钟 |
| 数据持久化 | 30分钟 |
| 测试调试 | 1小时 |
| **总计** | **约5.5小时** |

---

## 十一、后续扩展建议

### 11.1 体力溢出机制

```javascript
// 体力溢出时转化为金币
function onEnergyOverflow(overflowAmount) {
    const bonus = overflowAmount * 10; // 溢出1点体力 = 10金币
    player.gold += bonus;
    showToast(`体力溢出奖励: +${bonus}金币`);
}
```

### 11.2 体力双倍卡

```javascript
// 特定时段体力消耗翻倍
let doubleEnergyEvent = {
    active: false,
    multiplier: 2,
    endTime: 0
};
```

### 11.3 好友赠送体力

```javascript
// 好友每天可赠送1次体力
function sendEnergyToFriend(friendId) {
    // 调用微信社交API
}

function receiveEnergyFromFriend(friendId) {
    recoverEnergy(10);
}
```

### 11.4 看广告恢复体力

```javascript
// 观看广告恢复30点体力（每日次数限制）
let adEnergyCount = 0;
const MAX_AD_ENERGY_PER_DAY = 3;

function watchAdRecoverEnergy() {
    if (adEnergyCount >= MAX_AD_ENERGY_PER_DAY) {
        showToast('今日观看次数已用完');
        return;
    }

    wx.showRewardedVideoAd({
        // 广告回调
        onRewarded: () => {
            recoverEnergy(30);
            adEnergyCount++;
        }
    });
}
```

---

## 十二、附录：体力系统文案

### 12.1 体力不足弹窗文案

```
标题：体力不足
内容：
当前体力: {current}/{max}
通关需要: {cost}点

描述：
您的体力不足以挑战此关卡。
请等待体力恢复或使用道具补充。

按钮1：取消
按钮2：使用道具
按钮3：立即购买（跳转商城）
```

### 12.2 体力恢复提示

```
✅ 体力已恢复 +{amount}点
当前体力: {current}/{max}

💡 体力每5分钟恢复1点
   满体力需要: {time}分钟
```

### 12.3 体力不足提示

```
⚠️ 体力不足
挑战第{stage}关需要{amount}点体力
当前体力: {current}
```
