# 技能系统全面审计（基于 v1.1.15）

> **范围**：仅分析当前主游戏 `game - 副本.js` 中的技能体系（五行属性弹、衍生分支、质变节点、五行相生/克制、异常组合技、城墙血量承接、索敌与冷却逻辑）。
> **方法**：逐行核对 `SKILL_DEFS`、各 `recompute*Mods`、`updateBullets` 结算、`aimAngleForSlot`、五行常量与天赋注入，找出**逻辑矛盾**与**设计不足**。
> **状态**：本报告只做分析与建议，**未改动任何代码**。所有问题均标注代码行号，便于下一步定点修改。

---

## 一、速览评分卡

| 编号 | 类别 | 问题 | 严重度 |
|------|------|------|--------|
| C1 | 矛盾 | 五行「克制」系统完全失效（僵尸无 element，条件永假） | 🔴 严重 |
| C2 | 矛盾 | 五行「相生」图在已实现元素间断裂，火属性被孤立 | 🔴 严重 |
| C3 | 矛盾 | 龙卷风元素标识混乱（风 vs 木），且五行框架本就不全 | 🟠 中等 |
| C4 | 矛盾 | `getFreezeChance()` 是死代码（返回 1.0，从未被调用） | 🟡 轻微 |
| C5 | 矛盾 | `getSkillCooldown` 引用不存在的 `oil` 技能（死分支） | 🟡 轻微 |
| C6 | 矛盾 | 无目标时 CD 行为不一致（属性弹尊重、地雷/龙卷不尊重） | 🟠 中等 |
| D7 | 不足 | 按槽「打离墙第 n 近」在城墙被围时塌缩为同一点 | 🟠 中等 |
| D8 | 不足 | 干冰弹 +35% 基础伤害非对称（火/金无此补偿） | 🟠 中等 |
| D9 | 不足 | 干冰弹缺天赋起手等级（火/金有，水无） | 🟡 轻微 |
| D10 | 不足 | 相生分散惩罚使三元素 build 反受罚（0.95×） | 🔴 严重 |
| D11 | 不足 | 射击门限 + 竖直下落 = 反应窗口过窄 | 🟡 轻微 |
| D12 | 不足 | `attributeMods()` 漏聚合 lightning（陈旧 helper） | 🟡 轻微 |
| D13 | 不足 | 状态×元素增伤表极度不均（冰冻×火 = 翻倍） | 🟠 中等 |
| D14 | 不足 | 干冰弹已无「必冻」，绝对零度变成低概率处决 | 🟠 中等 |
| D15 | 不足 | 城墙重做后，聚怪/地雷等水平走位型技能被架空 | 🔴 严重 |

> 严重度图例：**🔴 严重**＝逻辑错误/无效系统/会误导平衡判断；**🟠 中等**＝平衡或设计张力，需决策；**🟡 轻微**＝一致性/命名，低优先。

---

## 二、关键矛盾（逻辑错误 / 死系统）

### C1 五行「克制（相克）」系统完全失效
- **现象**：`getElementBonus` 里有一段「攻击元素克制目标元素 → +30% 伤害」，但实战中这段加成永远不会触发。
- **证据**：
  - `WUXING_OVERCOME = { '火':'金', '金':'木', '木':'土', '土':'水', '水':'火' }`（`game - 副本.js:1431`）
  - 判定条件 `zombie.element && WUXING_OVERCOME[atkWx] === zombie.element`（`game - 副本.js:1472`）
  - 全代码搜索 `zombie.element =` **无任何赋值**（僵尸由 `zombieTypes` 生成，类型对象里不含 `element` 字段）。
- **影响**：五行克制维度是**纯死代码**。所有「金克木/水克火」的克制增伤都不会发生。玩家（和你）可能以为存在克制关系而在配装上做错误假设。
- **建议**：
  - 若想保留克制：给 `zombieTypes` 增加 `element` 字段（如 normal=木、fast=木、tank=土、boss=金），并在 `updatePendingSpawns` 生成时写入 `zombie.element`。
  - 若不想保留：直接删除 `WUXING_OVERCOME` / `WUXING_OVERCOME_BONUS` 及 `getElementBonus` 中对应分支，避免误导。

### C2 五行「相生」图在已实现元素间断裂，火属性被孤立
- **现象**：相生协同本意是「2 棵相生树最强、单树无、3+ 树衰减」。但已实现的元素只有 **火(爆炸) / 金(雷) / 水(冰)**，而相生链 `金→水→木→火→土→金` 中，木/土尚未实现，导致火属性在任何配对里都拿不到加成。
- **证据**：
  - `WUXING_GENERATE = { '火':'土', '土':'金', '金':'水', '水':'木', '木':'火' }`（`game - 副本.js:1436`）
  - `recomputeWuxingSynergy` 只在 `b = WUXING_GENERATE[a]` 且 `b` 也在已拥有元素集合时才计 1 对（`game - 副本.js:1452-1455`）。
  - 实测配对结果（已实现元素集 {火,金,水}）：
    - 金→水 ✅（雷+冰 是唯一有效相生对，+20%）
    - 火→土 ✗（土未实现）、水→木 ✗（木未实现）
  - 因此 **火(爆炸) 与雷或冰组队都是 0 对**，`wuxingSynergyMult = 1.0`。
- **影响**：
  - 雷+冰 = 1.20×，火+任意 = 1.0×。火属性在协同上被系统性孤立。
  - 这违背了「收集五行拿协同」的设计叙事，且让爆炸弹在高层 build 里相对弱势。
- **建议**：
  - 短期：把相生图改写为「已实现元素内部自洽」。例如明确「火生金、金生水、水生火」的三角环，使三元素两两都能成对（取 2 棵时 +20%，取 3 棵时计 2 对）。
  - 长期：补齐木/土属性树（或明确将龙卷风=木 纳入），让五行闭环成立。

### C3 龙卷风元素标识混乱（风 vs 木），且五行框架本就不全
- **现象**：龙卷风在技能定义为 `element:'风'`，但五行框架只有「金木水火土」，风不属于五行；代码又通过 `WUXING_ELEMENT['风']='木'` 把它归到木，同时 `getBulletElement` 又硬编码 `scores['木']`，注释写着「木(风=龙卷风)」。三处口径不一致。
- **证据**：
  - `SKILL_DEFS.tornado.element = '风'`（`game - 副本.js:1091`）
  - `WUXING_ELEMENT = { ..., '风':'木', '冰':'水' }`（`game - 副本.js:1428`）
  - `getBulletElement` 中 `scores['木'] = skills.tornado.level`（`game - 副本.js:1484`）
  - 注释「相生链 金→水→木→火 成立；木(风=龙卷风)」（`game - 副本.js:1433`）
- **影响**：玩家看到的是「风/龙卷风」，系统内部却是「木」。若 UI 暴露元素，会产生认知错位；且木作为「伤害树」从未真正落地（龙卷风是 cc 场域，不发射属性子弹），五行收集在战斗内实际上是残缺的。
- **建议**：统一口径——要么把龙卷风对外显示为「木(风)」并补全木属性伤害树，要么接受龙卷风是「非五行 CC」并把它从 `WUXING_ELEMENT` 的元素集合里排除（避免它虚假参与相生计数）。

### C4 `getFreezeChance()` 是死代码
- **现象**：函数名为「获取冻结概率」，返回硬编码 `1.0`，但全文件仅此一处定义，无任何调用。
- **证据**：`game - 副本.js:1271`（定义）；grep 全文无其他引用。
- **影响**：真实的冻结概率由 `getAoeFreezeChance()`（冰川+冰霜新星）和 `novaFreezeChance`（冰霜新星主目标）承担。这个 1.0 的死函数极易被误读为「干冰弹基础必冻」，与 v1.1.11 之后「基础不再冻结」的设计相冲突。
- **建议**：删除该函数；如需占位说明，改注释即可。

### C5 `getSkillCooldown` 引用不存在的 `oil` 技能
- **现象**：冷却 UI 函数里有 `if (key === 'oil')` 分支，但 `SKILL_DEFS` 中**没有 `oil` 技能**（油渍已在早期并入「引燃」，见火树 `ignite`）。
- **证据**：`game - 副本.js:3088`；`SKILL_DEFS` 仅有 `mine / tornado` 两个非子弹技能（`game - 副本.js:1089-1092`）。`drawSkillUI` 遍历 `acquiredSkills`，永远不会含 `oil`，故该分支永不触发。
- **影响**：死分支，无功能危害，但属于「重构残留」，会误导后续维护者以为油渍仍是可获取技能。
- **建议**：删除 `oil` 分支（及 `OIL_*` 相关未使用常量若已无引用）。

### C6 无目标时 CD 行为不一致
- **现象**：你此前要求「没有可射击目标时技能 CD 不转」。属性弹（爆炸/干冰/雷）已遵守——`if (hasShootable && now - s._lastFire >= cd)`（`game - 副本.js:5481`）。但**地雷、龙卷风**的 CD 仍由 `updateFields` 的定时器无条件推进，冷却环照常转动，即使场上空无一怪。
- **证据**：属性弹门控在 `game - 副本.js:5477-5485`；地雷/龙卷风的释放走 `MINE_SPAWN_INTERVAL` / `TORNADO_RELEASE_CD` 计时器（不受 `hasShootable` 控制）；`getSkillCooldown` 对这两者照常返回进度（`game - 副本.js:3087-3089`）。
- **影响**：同屏技能里，一部分「没目标不转 CD」、另一部分「没目标照转」，玩家会困惑「为什么这个技能圈一直在转却不放」。
- **建议**：明确一条统一规则——
  - 若坚持「部署类技能应按自身节奏循环」：在冷却环 UI 上对这类技能加注释/不同底色，向玩家解释「这是部署 CD，与目标无关」。
  - 若希望一致：给地雷/龙卷风也加 `hasShootable` 门控（但会改变它们的铺场节奏，需重新平衡）。

---

## 三、设计不足（平衡 / 系统性张力）

### D7 按技能槽「打离墙第 n 近」在城墙被围时塌缩
- **现象**：你设计的「五行子弹按解锁顺序打离墙第 n 近」很优雅，但僵尸竖直下落、钉墙后 `wallDist ≈ 0` 对所有人相同，`aimAngleForSlot` 的排序会塌缩到 `playerDist` 排序——于是所有五行弹最终都瞄准最靠近坦克的同一簇怪，「分层覆盖」只在下落阶段短暂存在。
- **证据**：`aimAngleForSlot` 排序 `(a.wallDist - b.wallDist) || (a.playerDist - b.playerDist)`（`game - 副本.js:3966`）；城墙钉墙逻辑使 `z.y+radius >= WALL_Y` 后几乎全部贴墙（v1.1.13 城墙玩法）。
- **影响**：城墙一旦被围（这是常态），第 2/3 槽分层索敌机制基本失效，五行弹扎堆，与「分层次覆盖不同深度」的初衷不符。
- **建议**：
  - 备选 A：把排序键改为「到墙的**时间**」而非「到墙的距离」——即预测谁最先砸墙，让各槽锁定不同「即将破墙」的目标，即使在墙下也能分层。
  - 备选 B：改为按**横向 x 分桶**，让第 n 槽锁定屏幕第 n 个水平区段的最近怪，保证空间上分散覆盖。

### D8 干冰弹 +35% 基础伤害非对称
- **现象**：只有干冰弹享受 `WATER_BASE_DMG_MUL = 1.35` 的平乘基础伤害，爆炸/雷没有对应补偿。
- **证据**：`const WATER_BASE_DMG_MUL = 1.35`（`game - 副本.js:1248`）；仅 `type==='freeze'` 时乘入（`game - 副本.js:4020-4021`）。
- **影响**：干冰弹同时拥有「+35% 直伤 + 必减速 + 范围冰爆 + 概率冻结 + 绝对零度处决」，单树综合强度显著高于火/金。结合 D15（城墙下冻结=停掉破墙伤害），干冰弹在城墙版本里过强。
- **建议**：做一次三树 DPS/控制横评。若确认干冰弹超模，要么把 +35% 改为三树共有（火/金也各 +X%），要么把干冰弹的补偿改为「控制向」而非「直伤向」（例如把 1.35 降到 1.15，把差额补给冻结时长/冰爆半径）。

### D9 干冰弹缺少天赋起手等级
- **现象**：开局时 `skills.explosive.level += talentMods.explosiveLevel` 和 `skills.lightning.level += talentMods.lightningLevel` 都有，但**没有** `skills.freeze.level += talentMods.freezeLevel`。
- **证据**：`game - 副本.js:5271-5272`（仅 explosive / lightning）；`getFreezeDuration` 虽用到 `talentMods.freezeLevel`（`game - 副本.js:1277`），但那只影响冻结时长，不给起手等级。
- **影响**：若天赋系统存在「干冰弹等级」天赋，它只对已有干冰弹的局生效（且只加时长），不像火/金那样提供**开局即有的等级领先**。干冰弹在天赋维度被隐性削弱。
- **建议**：与火/金保持一致，增加 `skills.freeze.level += talentMods.freezeLevel`（前提是天赋里确有对应项；若无，则补一个或删除其余两行的天赋注入以保持对称）。

### D10 相生分散惩罚使三元素 build 反受罚
- **现象**：`wuxingSynergyMult = 1 + 0.20*pairs - 0.25*(treeCount-2)`。取三元素（火金水）时最多 1 对 → `1 + 0.20 - 0.25 = 0.95`，即**拿第三个元素反而全场属性伤害 ×0.95（被惩罚）**。
- **证据**：`game - 副本.js:1456-1458`。
- **影响**：与「收集五行变强」的叙事直接矛盾。玩家集齐三元素不仅没奖励，还被削 5%；且因为木/土缺失，三元素最多只能拿到 1 对（见 C2），根本到不了 2 对的甜蜜点。这会惩罚全收集玩家。
- **建议**：
  - 降低或移除分散惩罚（如 `WUXING_SPREAD_PENALTY` 由 0.25 降到 0.10，或仅在 >3 树时生效）。
  - 配合 C2 把相生图改成三角环，使 3 树能拿 2~3 对，让「全收集」成为正收益。

### D11 射击门限 + 竖直下落 = 反应窗口过窄
- **现象**：`zombieShootable` 要求 `z.y > TANK_FIRE_LINE_Y`（约屏幕 25% 处）才可被锁定，而城墙在 82% 处。僵尸从顶部落下，前 75% 路程完全不可被坦克射击。
- **证据**：`TANK_FIRE_LINE_Y = screenHeight * 0.25`；`zombieShootable` 门控；竖直下落自 v1.1.13。
- **影响**：坦克对「正在逼近」的怪几乎没有预警射击窗口，「离墙第 n 近」的排序只在这 25%~82% 的薄带里真正有区分度，进一步削弱 D7 的分层价值。
- **建议**：评估是否把开火线 `TANK_FIRE_LINE_Y` 上移（如 0.15H），或在下落途中允许「预警射击」（打未到线的怪但不计入钉墙前的伤害衰减），给玩家更早的反馈与控制感。

### D12 `attributeMods()` 漏聚合 lightning（潜在陈旧 helper）
- **现象**：`attributeMods()` 只聚合 `explosive` 和 `freeze` 的 `_mods`，**没有** `lightning`。
- **证据**：`game - 副本.js:1495-1507`（`const all = [skills.explosive..., skills.freeze...]`，缺 `skills.lightning`）。
- **影响**：该函数仍是 `getCritChance / getCritMult / getEffBulletCount/Speed/Piercing` 的默认回退（`game - 副本.js:1198-1208`）。`spawnSplitBullets` 用 `getEffBulletSpeed()`（经 `attributeMods`），纯雷 build 的「急速冷却」提速不会作用到分裂弹。当前主弹走的是 `attrModsForType(type)`（正确），所以主弹不受影响；但该 helper 是**第三棵树加入时未同步更新的陈旧聚合**，存在隐患。
- **建议**：把 `skills.lightning._mods` 并入 `attributeMods()`；或直接确认这些 `getEff*` 是否还有必要存在（多数已被 per-tree 逻辑取代），若仅 `spawnSplitBullets` 用速度，可让它直接读 `player.bulletSpeed`。

### D13 状态×元素增伤表极度不均
- **现象**：`STATUS_ELEMENT_BONUS` 中「冰冻中受火伤 +100%」远高于「减速中受金伤 +30%」「灼烧中受风伤 +20%」。
- **证据**：`game - 副本.js:1311-1315`。
- **影响**：火属性踩中「冰冻」时直接翻倍（×2.0），而金/风的状态联动只 +20~30%。这放大了「冰+火」组合的地位，且与 C2/D10 冲突——你想用组合技鼓励多元素，但相生惩罚又 discourages 多元素。两套系统互相打架。
- **建议**：把状态×元素表拉平到同一量级（如统一 +30~50%），或明确「冰火融化」是旗舰组合、其余为小协同，并在 UI 上把这层关系讲清楚，避免数值失衡。

### D14 干冰弹已无「必冻」，绝对零度变成低概率处决
- **现象**：v1.1.11 起干冰弹基础不再必冻，冻结只来自「冰霜新星（概率）/ 极寒领域（概率）」分支。于是 `绝对零度（glacialDoom）` 的「对被冻结目标追加最大生命%」也只在概率冻结命中时才可能触发。
- **证据**：冰霜新星 `frostNovaFreezeChance += 0.15*bl`（`game - 副本.js:1081`）；主目标冻结 `novaFreezeChance` 仅冰霜新星提供（`game - 副本.js:4299-4303`）；`glacialDoom` 依赖 `zombie.frozenUntil > nowHit`（`game - 副本.js:4307`）。
- **影响**：作为「单树后期核心」的处决技，现在触发高度依赖随机冻结，手感上可能「偶尔一发秒杀、大部分时间不触发」，爽点不稳定。
- **建议**：给干冰弹保留一个**低频必冻锚点**（比如每 N 发或主目标命中累计满阈值必冻一次），让绝对零度有稳定节奏，而非纯赌概率；或把 `glacialDoom` 改为「对减速目标也生效但倍率减半」，降低对冻结的硬依赖。

### D15（Level Designer 视角）城墙重做后，聚怪/地雷等水平走位型技能被架空
- **现象**：城墙玩法把僵尸改为**竖直下落、到墙即钉死**，战场从「水平推进、有走位与车道」变成「垂直瀑布、全宽堆积」。而地雷（静态圆）、龙卷风（圆形 CC、无牵引位移）原本是为**水平移动**的敌群设计的。
- **证据**：竖直下落 + 钉墙（`updateZombies`，v1.1.13）；龙卷风 `只减速/灼烧，不牵引位移`（早期结论）；`randomFieldPos` 把地雷撒在 `top..bot-110` 的随机点（`game - 副本.js:1183-1191`）。
- **影响**：从空间设计看，这是一次**流程重构**：原来的「关键路径=僵尸行进路线、 chokepoint=坦克前方」被抹平。地雷不再需要「预判走位」去埋，龙卷风不再能「聚怪成团吃 AoE」，二者价值被显著稀释。城墙成了唯一的空间锚点，所有战术都收敛到「城墙前这一段」。
- **建议**：
  - 给龙卷风加**竖直方向上的牵引/抬升**（让被卷怪暂时离开城墙、延缓破墙），使其重新有「争取时间」的战略意义；
  - 让地雷改为「沿城墙线布设」或「在僵尸落点预测处布雷」，契合垂直下落的新空间逻辑；
  - 或反过来：在城墙前保留一条窄「突进通道」，让部分僵尸仍走水平路由，恢复走位与 chokepoint 的空间张力。

---

## 四、修改建议优先级路线图

### P0（必须修的逻辑错误，建议下个版本处理）
1. **C1** 五行克制失效 → 给僵尸加 `element`，或删除克制系统。
2. **C2 / D10** 相生图断裂 + 分散惩罚 → 改写为三角环（火↔金↔水 两两相生），降/移分散惩罚，让多元素为正收益。
3. **C5** 删除 `oil` 死分支。

### P1（平衡与系统一致性，需你拍板）
4. **D8 / D9** 三树基础伤害与天赋起手对齐，做横评。
5. **D13 / D14** 状态×元素表拉平；给绝对零度一个稳定触发锚点。
6. **C6** 统一「无目标是否转 CD」的全局规则。
7. **D15** 决定城墙版本下 CC/地雷技能的空间定位（牵引 / 落点布雷 / 保留突进通道）。

### P2（一致性 / 长期）
8. **C3** 统一龙卷风元素口径（风 or 木），并补齐木/土或明确其非五行 CC。
9. **C4** 删除 `getFreezeChance()` 死代码。
10. **D7 / D11** 重做索敌分层键（按到墙时间 / 横向分桶），上移开火线给更早反馈。
11. **D12** 同步 `attributeMods()` 聚合 lightning，或清理陈旧 `getEff*` helper。

---

## 五、附：已确认健康、无需改动的部分（避免误改）
- **属性弹独立 CD + 急速冷却分支**：设计清晰，`getAttrReleaseCd` 封底 `min`，三树各自独立，逻辑自洽。
- **衍生分支的 reqLevel / prereq / mutex 互斥体系**：结构正确，互斥同档二选一已实现（如冰川⇄霜寒、冰霜新星⇄极寒领域、破甲⇄引燃）。
- **质变节点 qualNodes**：升级到指定等级触发一次、去重（`fireQualNodes`）逻辑正确。
- **致命暴击已统一为各树「暴击」分支**：不再有独立暴击技能，`getCritChance/Mult` 按子弹自身树取，互不干扰，正确。
- **城墙血量承接 + 钉墙攻击间隔**：`WALL_MAX_HEALTH=600` 与 `WALL_ATTACK_INTERVAL=500ms` 已把耗血压到低于旧模型，方向正确（其余平衡见 D8/D15）。
- **不朽之身 / 护盾减伤 / 复活无敌**：`updateZombies` 中保留完整，`gameOver` 与无敌期门控正确。

---

> **下一步**：以上均为分析。若确认修改方向，建议从 P0 三项（C1、C2+D10、C5）入手，它们不改变手感、只修正「无效系统」和「误导性的死代码」，风险最低、收益最明确。
