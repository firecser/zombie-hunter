# 2026-08-07 工作日志

## 炸弹爆炸音效增强（修复，未提交）
- 问题：原 `AudioSystem.playBombExplosion()` 用 1 个 sawtooth 振荡器 + 3 个低频 sine，经 `sfxGain`(0.3) 后太弱，且不"像"爆炸。
- 修复：重写为合成爆炸声——①白噪声主体（低通频率 2200→150Hz 快扫，制造"轰"）；②低频 thump（140→38Hz sine，身体冲击感）；③中频碎片层（带通噪声短促碎裂）。新增独立 `boomBus`(增益 0.6) 直连 `ctx.destination`，绕过低增益 sfxGain，更有冲击力；保留 `createBuffer` 不支持时的振荡器降级。
- 覆盖范围：`useBomb()`（清屏大炸弹）与第3303行 explosive 子弹爆炸均调用同一函数，一并增强。
- 校验：`node --check` 双文件通过，`game.js` 已 cp 同步。改动**未提交**（下一个版本 v1.0.61）。

## 爆炸音效二次微调（更浑厚、降尖锐、加长尾，未提交）
- 用户反馈：上一版不够浑厚、偏尖锐、时长稍短。
- 调整：噪声主体低通起始 2200→1100Hz、扫到 70Hz、时长 1.1→1.6s；新增超低频 sub 层（70→28Hz，1.0s 长尾）增加浑厚；碎片层中心频率 1400→700Hz、增益 0.4→0.18、时长 0.35→0.25s（大幅削弱尖锐）；thump 拉长到 0.7s；boomBus 0.6→0.55（防过度削波）。
- 校验：`node --check` 双文件通过、已同步。仍为 v1.0.61 未提交内容。

## 13个内嵌小游戏音效补充（音效下载器 skill，未提交）
- 用「音效下载器」skill（sounds-mp3.com 优先 → poppop.ai → mixkit.co）下载 13 个 MP3 到 `sounds/` 目录，全部免费商用无需署名，总约 2.9MB（主包 game.js 417K+images 330K+sounds 2.9M≈3.65M < 4MB 限制，安全）。
- 跳过 mixkit 的 WAV（微信偏好 MP3），slide 改用 sounds-mp3.com 的 "swoosh" MP3；jump(20K)/place(21K) 用体积优先策略压到最小，避免超包。
- 13 个文件名：slide/merge/slash/jump/flap/correct/wrong/hit/coin/place/shoot/win/lose。
- 新增 `MiniGameAudio` 播放器（`wx.createInnerAudioContext`，src=`sounds/{name}.mp3`，受 `soundEnabled`/`AudioSystem.isMuted` 控制，带 cache）。
- 接入点（各动作函数内 `MiniGameAudio.play(...)`，win/lose 用 `if(!g.gameOver)` 防重复）：g2048Move、gQiexiguaSlice、gFeidegenggaoUpdate、gBunengsiTap、gXnfFlap、handleMiniGameQmxzInput、handleMiniGameBdsjmInput、gZqylTap、gQingwaTap、gSqsdTap、gSjmaoTap、gYbhTap、gDlsqDragEnd。
- 校验：node --check 双文件通过，diff 一致；fake wx 环境冒烟测试确认 13 个 src 正确映射、静音/soundEnabled=false 均抑制播放（SMOKE OK）。仍为 v1.0.61 未提交内容。

## 修订：13个小游戏音效改为 Web Audio 合成（方案A，替代 MP3）
- 用户反馈 2.9MB 偏大（主包 4MB 红线已用 ~3.85MB，余量仅 ~150KB）。主游戏 AudioSystem 本就是纯 Web Audio 合成、零音频文件，故统一改为合成。
- 重写 `MiniGameAudio`：去掉 `wx.createInnerAudioContext` + `sounds/*.mp3`，改为复用 `AudioSystem` 的 `ctx`/`sfxGain`（避免多建 AudioContext），用 `_tone`(振荡器扫频+包络) 与 `_noise`(噪声+滤波扫频) 两个基元合成 13 个语义音效（slide/merge/slash/jump/flap/correct/wrong/hit/coin/place/shoot/win/lose），用 `_fx` 派发表 + `play(name)` 调度；delay 参数做多音精确排程（win/coin/correct/merge 的连音）。
- 删除 `sounds/` 目录（git rm -r）。主包音效体积回到 ≈0（总包 ~775KB，余量 ~3.2MB）。
- 13 处 `MiniGameAudio.play('xxx')` 调用点无需改动（接口签名保持 `play(name)`）。
- 校验：git rm sounds + cp 同步、node --check 双文件通过；fake wx(mock Web Audio ctx) 冒烟测试 13 个 name 均生成 oscillator/gain 节点、静音与 soundEnabled=false 均抑制、未知 name 安全跳过（SMOKE OK）。
- 已把 v1.0.61 提交 amend 为合成版并移动 tag（仍**未推送**）。

## PAT 恢复明文 + v1.0.61 推送
- 用户重发真实 PAT；之前 MEMORY.md 第71行存的是 `[REDACTED_PAT]` 占位符（原始字节确为占位符，非显示脱敏），已用 Edit 写回真实 40 字符 token。
- 推送：node 脚本内联 `https://<PAT>@github.com/firecser/zombie-hunter.git`，`GIT_TERMINAL_PROMPT=0`，输出脱敏。**RC=0 成功**；`git ls-remote` 确认 `v1.0.61` 已上远端。
- 注意：本机 hosts 把 github.com→127.0.0.1，推送依赖代理/VPN 可达（本次成功）。

## 导出「战斗中三选一技能」Excel（数值策划）
- 用户用 @游戏数值策划 skill 要求整理战斗中三选一技能的技能清单/等级/每级变化，输出 Excel。
- 数据来源：`game - 副本.js` 的 `skills`(811) + `upgradePool`(852，共 12 个技能) + 战斗公式（2600-2700 行 getter）+ `applyUpgrade`(3168，伤害/射速/多重/弹速/穿透/生命的每级折入)。
- 关键事实：局内三选一技能**无硬性等级上限**（可反复选叠级），仅 `MAX_SKILLS=5` 限制"同时持有不同技能数"；子弹特效类有软上限（暴击率≤60%、护盾减伤≤80%、冰冻率≤50%/时长≤2600ms、减速率≤60%/系数≥0.3）。
- 真实公式：爆炸半径=40+20×等级；闪电链目标=等级+1；火力强化初始即 Lv1（基础伤害10）。
- 产出：`三选一技能表.xlsx`（Sheet1 总览 12 技能/分类/初始等级/每级提升/软上限；Sheet2 每级数值 Lv1-10 用公式递推）。用受管 venv(openpyxl) 生成；LibreOffice 重算脚本在 Windows(AF_UNIX)不可用，已用 Python 按真实公式复算校验。
- 临时生成脚本 `gen_skill_xlsx.py` 已清理。

## 网络调研《向僵尸开炮》战斗技能（参考，非本项目）
- 用户澄清：上一份「三选一技能表.xlsx」是本项目(zombie-hunter)战斗技能，需保留；本次要的是**网络上《向僵尸开炮》(xjskp)真实游戏**的战斗技能设计数据作参考。
- 数据来源：官网 xjskp.scgame.com.cn（技能进化组合）+ tc98 攻略（13 核心技能 2/6/10/14/18/22/26/31 级解锁明细）。
- 关键结论：13 个核心战斗技能（温压弹/干冰弹/电磁穿刺/装甲车/高能射线/制导激光/冰暴发生器/跃迁电子/旋风加农/空投轰炸/压缩气刃/燃油弹/无人机冲击），**最高 31 级**；局内升级在关键等级节点弹出"三选一"选项解锁进化分支；另有核心(皮肤)系统 6 个 + 4 个命中触发的进化组合(温压序列/电磁引导/零度风暴/追加气刃)。
- 产出：`向僵尸开炮技能参考.xlsx`（3 表：核心技能总览 / 技能升级解锁明细 / 核心系统与进化组合）。受管 venv(openpyxl) 生成，已校验 3 表、13 技能×8 节点=104 行明细完整。临时脚本 gen_xjskp.py 已清理。
- 注意：这是参考资料，未改动游戏代码、未提交 git。

## 技能策略深度对照分析（对标《向僵尸开炮》）
- 用户要求做一版对照分析，目标：本项目(zombie-hunter)技能策略深度达到《向僵尸开炮》水平。
- 数据来源：本项目 12 个局内升级技能（game - 副本.js 811-823/852-864，全为子弹特效/角色属性类，无等级上限仅 MAX_SKILLS=5）+ 刚生成的《向僵尸开炮》参考表（13 核心技能 / 31 级 / 六系 / 状态交互 / 核心+宝石系统）。
- 核心差距（4 处，优先级高）：① 缺战场部署类技能(地面/光束/聚怪/飞行) ② 无元素系别与流派 ③ 无异常状态交互/联动(冰火叠伤/电网麻痹) ④ 无质变等级节点。
- 产出：`技能策略深度对照分析.xlsx`（4 表：维度对照总表/技能大类覆盖对比/异常状态与联动/借鉴落地路线 Roadmap 分 MVP·中期·长期）。本地 openpyxl 生成，未改游戏代码、未提交 git。临时脚本已清理。

## 技能策略深度改造 · 分阶段落地计划（用户要求"逐步完成三条路线"）
- 用户确认：以《向僵尸开炮》设计为准改造本项目技能系统；MVP(阶段1)与中期(阶段2)做到可实施级详细设计，长期(阶段3,6周+)只搭架构、等前两条完善后再做。
- 已定位现有架构（行号）：`skills`(811-824,12个)/`upgradePool`(852-865)/`getCrit*/getFreeze*/getSlow*/getShield*`(873-895)/`applyUpgrade`(3168)/`showUpgradePanel`(3151)/`talentMods`+`applyTalentsToBattle`(3866/3879)。
- 计划要点（已建任务 #77-#80）：
  - #77 基础重构：收敛为单一 `SKILL_DEFS` 注册表（id/name/icon/element/category/maxLevel/tier/effect/qualNodes/synergy），运行时只留 skillLevels{} + acquiredSkills；talentMods 保持兼容。不起玩法变化，只为集中加技能/元素/联动入口。
  - #78 Phase1 MVP：12 技能打 element 标签 + 新增 3 部署/聚怪技能（地雷 mine/油渍 oil 含 burningUntil/龙卷风 tornado 聚怪），upgradePool→15，MAX_SKILLS→6-7，新技能 effect 接入主循环 tick。
  - #79 Phase2 中期：异常交互 getElementBonus(冰冻+火伤+100%/减速+雷伤+30%) + qualNodes 质变节点(Lv3/Lv5 一次性强化) + comboEffects 命中组合技 + stunUntil 硬控眩晕。
  - #80 Phase3 长期仅架构：CORE_DEFS 核心/皮肤骨架、buildMods 数据层、ELEMENT_COUNTER 元素克制表，全预留空挂载、不实装玩法。
- 每阶段收尾：编辑副本.js→cp game.js→node --check→自测→本地提交+打 tag(v1.0.62 起)→问推送→上传前先 DevTools「全部加固」。
- 用户已拍板（"1，认可 2，一起落地"）：认可计划并指示 #77+#78 一起落地实施。

## #77+#78 编码落地完成（v1.0.62，本地已 cp 同步，未提交）
- 用户指令"1，认可 2，一起落地" → 落地 #77(基础架构重构) + #78(Phase1 MVP)。
- **#77 SKILL_DEFS 注册表重构**（零玩法变化、向后兼容）：
  - 原分散的 `skills` 字面量(811) + `upgradePool` 字面量(852) + `applyUpgrade` switch(3168) 三处合并为单一 `SKILL_DEFS` 注册表（每个技能声明 type/name/icon/element/category/maxLevel/desc/apply(lv)）。
  - 运行时 `skills` 改为 `SKILL_DEFS` 派生实例，仅持 level 等运行时字段；`upgradePool` 改为 `SKILL_DEFS` 键派生（共 15）。
  - `applyUpgrade` 去 switch，改 `_def = SKILL_DEFS[type]; _def.apply(skills[type].level)`，并注释预留 `qualNodes`(Phase2 接入)。
  - 存档兼容：`skills[key].level` 语义不变（JSON 自然丢函数），新技能加载默认 level 0；`talentMods`/`applyTalentsToBattle` 仍折入 `skills[type].level`，天赋路径不变。
- **#78 Phase1 MVP**（12 技能加 element/category 标签 + 3 部署/聚怪技能）：
  - 元素系别（火/冰/雷/风/物理）× 类别（bullet/buff/field/cc），对标《向僵尸开炮》流派。
  - 新增 `MAX_SKILLS = 5 → 7`（放宽槽位饥饿）。
  - 3 个新技能接入主循环：`mine`(地雷/物理/field)、`oil`(油渍/火/field，踩中灼烧 burningUntil)、`tornado`(龙卷风/风/cc，聚怪牵引)。
  - `startGame` 清空 `mines/oilPatches/tornadoes`；新增 `updateFields(dt)`（周期生成+地雷踩爆 AOE+油渍灼烧 DOT 逆序遍历调用 damageZombie）+ `drawFields()`（油渍/地雷/龙卷风绘制），在 playing/upgrade 两处渲染调用。
  - 灼烧 DOT 在 `updateZombies(dt)` 末尾逆序 pass（避免 for...of 跳杀）；用 player.damage*0.08/500ms tick。
- **自测**：`node --check` 双文件 SYNTAX OK；无头冒烟测试 `smoke_fields.js`（stub wx/canvas/audio，真实跑 startGame+15 技能各升3级+200帧update）两次通过，定向校验 `mineExploded=true`（地雷爆炸分支）、`burnHealth=999`（油渍灼烧使僵尸 1000→999，damageZombie 结算生效）。
- **收尾**：`game - 副本.js`(11052行) cp → `game.js`(11052行) 同步完成、node --check OK；临时 `smoke_fields.js` 已删除。下一步：本地提交 + 打标签 **v1.0.62**，排除无关 xlsx 锁文件；#79(Phase2 中期)/#80(Phase3 长期) 保持 pending。
- ⚠️ 上传微信提审前：`game.js` 当前为可读明文，需先 DevTools 右键 →「全部加固」再上传；上传完把 `game - 副本.js` 拷回 `game.js` 恢复可读源码。
