# 2026-08-07 工作日志

## 炸弹爆炸音效增强（修复，未提交）
- 问题：原 `AudioSystem.playBombExplosion()` 用 1 个 sawtooth 振荡器 + 3 个低频 sine，经 `sfxGain`(0.3) 后太弱，且不"像"爆炸。
- 修复：重写为合成爆炸声——①白噪声主体（低通频率 2200→150Hz 快扫，制造"轰"）；②低频 thump（140→38Hz sine，身体冲击感）；③中频碎片层（带通噪声短促碎裂）。新增独立 `boomBus`(增益 0.6) 直连 `ctx.destination`，绕过低增益 sfxGain，更有冲击力；保留 `createBuffer` 不支持时的振荡器降级。
- 覆盖范围：`useBomb()`（清屏大炸弹）与第3303行 explosive 子弹爆炸均调用同一函数，一并增强。
- 校验：`node --check` 双文件通过，`game.js` 已 cp 同步。改动**未提交**（下一个版本 v1.0.61）。

## 爆炸音效二次微调（更浑厚、降尖锐、加长尾，未提交）
- 用户反馈：上一版不够浑厚、偏尖锐、时长稍短。
- 调整：噪声主体低通起始 2200→1100Hz、扫到 70Hz、时长 1.1→1.6s；新增超低频 sub 层（70→28Hz，1.0s 长尾）增加浑厚；碎片层中心频率 1400→700Hz、增益 0.4→0.18、时长 0.35→0.25s（大幅削弱尖锐）；thump 拉长到 0.7s；boomBus 0.6→0.55（防过度削波）。
- 校验：`node --check` 双文件通过、已同步。仍为 v1.0.61 未提交内容。

## 13个内嵌小游戏音效补充（音效下载器 skill，未提交）
- 用「音效下载器」skill（sounds-mp3.com 优先 → poppop.ai → mixkit.co）下载 13 个 MP3 到 `sounds/` 目录，全部免费商用无需署名，总约 2.9MB（主包 game.js 417K+images 330K+sounds 2.9M≈3.65M < 4MB 限制，安全）。
- 跳过 mixkit 的 WAV（微信偏好 MP3），slide 改用 sounds-mp3.com 的 "swoosh" MP3；jump(20K)/place(21K) 用体积优先策略压到最小，避免超包。
- 13 个文件名：slide/merge/slash/jump/flap/correct/wrong/hit/coin/place/shoot/win/lose。
- 新增 `MiniGameAudio` 播放器（`wx.createInnerAudioContext`，src=`sounds/{name}.mp3`，受 `soundEnabled`/`AudioSystem.isMuted` 控制，带 cache）。
- 接入点（各动作函数内 `MiniGameAudio.play(...)`，win/lose 用 `if(!g.gameOver)` 防重复）：g2048Move、gQiexiguaSlice、gFeidegenggaoUpdate、gBunengsiTap、gXnfFlap、handleMiniGameQmxzInput、handleMiniGameBdsjmInput、gZqylTap、gQingwaTap、gSqsdTap、gSjmaoTap、gYbhTap、gDlsqDragEnd。
- 校验：node --check 双文件通过，diff 一致；fake wx 环境冒烟测试确认 13 个 src 正确映射、静音/soundEnabled=false 均抑制播放（SMOKE OK）。仍为 v1.0.61 未提交内容。

## 修订：13个小游戏音效改为 Web Audio 合成（方案A，替代 MP3）
- 用户反馈 2.9MB 偏大（主包 4MB 红线已用 ~3.85MB，余量仅 ~150KB）。主游戏 AudioSystem 本就是纯 Web Audio 合成、零音频文件，故统一改为合成。
- 重写 `MiniGameAudio`：去掉 `wx.createInnerAudioContext` + `sounds/*.mp3`，改为复用 `AudioSystem` 的 `ctx`/`sfxGain`（避免多建 AudioContext），用 `_tone`(振荡器扫频+包络) 与 `_noise`(噪声+滤波扫频) 两个基元合成 13 个语义音效（slide/merge/slash/jump/flap/correct/wrong/hit/coin/place/shoot/win/lose），用 `_fx` 派发表 + `play(name)` 调度；delay 参数做多音精确排程（win/coin/correct/merge 的连音）。
- 删除 `sounds/` 目录（git rm -r）。主包音效体积回到 ≈0（总包 ~775KB，余量 ~3.2MB）。
- 13 处 `MiniGameAudio.play('xxx')` 调用点无需改动（接口签名保持 `play(name)`）。
- 校验：git rm sounds + cp 同步、node --check 双文件通过；fake wx(mock Web Audio ctx) 冒烟测试 13 个 name 均生成 oscillator/gain 节点、静音与 soundEnabled=false 均抑制、未知 name 安全跳过（SMOKE OK）。
- 已把 v1.0.61 提交 amend 为合成版并移动 tag（仍**未推送**）。
