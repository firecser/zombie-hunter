# 2026-08-05 工作日志

## 修 bug：内嵌小游戏结算遮罩遮挡底部按钮（v1.0.36）
用户反馈：围住神经猫(sjmao) 的失败/胜利结算遮罩把底部「返回/新游戏」按钮挡住（但按钮仍能按到）；并称暴打神经猫(bdsjm)、全民寻找房祖名(qmxz) 之前也出现过，要求一并排查后面添加的 10 款小游戏。

### 根因
按钮在 draw 函数**开头**先绘制，结算遮罩用**全屏** `ctx.fillRect(0, 0, screenWidth, screenHeight)` 在结尾覆盖，把按钮视觉盖住。点击逻辑基于坐标 `inRect`，与绘制层级无关 → 按钮"还能按到"。

### 统一排查 13 款内嵌小游戏（drawMiniGame*）的结算遮罩
| 游戏 | 遮罩写法 | 按钮 | 结果 |
|---|---|---|---|
| 2048 | drawMiniGameOverlay 只盖棋盘 | 不挡 | ✅ |
| qmxz/bdsjm | 只盖游戏区 `fillRect(0,0,W,btnTop-8)` | 不挡 | ✅ 此前已修 |
| qiexigua/feidegenggao/bunengsi | 只盖 board 区 | 不挡 | ✅ |
| xnf(小鸟飞飞飞) | 全屏遮罩，但按钮在遮罩**之后**置顶重绘 | 不挡 | ✅ 安全（注释"HUD 始终置顶"） |
| **sjmao/zqyl/qingwa/sqsd/ybh/dlsq** | **全屏遮罩** | **被挡** | ❌ 本次修复 |

### 修复
- 给 6 个函数（drawMiniGameSjmao/Zqyl/Qingwa/Sqsd/Ybh/Dlsq）在 `const L = ...Layout()` 后加 `const btnTop = Math.min(L.backBtn.y, L.restartBtn.y);`
- 把 8 处结算 `fillRect(0, 0, screenWidth, screenHeight)` 改为 `fillRect(0, 0, screenWidth, btnTop - 8)`（qingwa/ybh 各 2 处 win+stuck，其余各 1 处）。
- 注意：Xnf 的 7776 全屏遮罩**故意保留**（按钮置顶重绘，已安全）；主游戏背景的全屏 fillRect 不动。
- node --check 双文件通过；game.js 由副本 cp 同步。commit 9494245，tag v1.0.36，**未推送**（等用户指令）。

### 经验（可复用）
内嵌小游戏结算遮罩标准二选一：
1) 遮罩只盖游戏区，留底部按钮栏：`ctx.fillRect(0, 0, screenWidth, btnTop - 8)`；
2) 全屏遮罩时，按钮必须在遮罩**之后** `drawMiniGameButton(...)`（如 xnf 的 HUD 置顶写法）。
避免"按钮先画 + 全屏遮罩后盖"的组合。

## 优化：大力射手（dlsq）球位置 + 背景（v1.0.37）
用户反馈：大力射手初始球太偏左下角，玩家按住球向后拉（向下/向左蓄力）的空间不够；且背景太单一，要求用主游戏的背景图。

### 改动
- 球起始位置 `gDlsqBallPos()`：由 `x=0.22*boardW, y=boardH-40`（贴底）改为 `x=0.26*boardW, y=0.50*boardH`（棋盘中部偏左）。
  - 计算验证：320×568 最小屏 maxPull≈214px、375×667≈265px，均 ≥180px（满力阈值）；原贴底仅约 80px（≈44% 力）。
- 背景：`drawMiniGameDlsq()` 内 `drawRoyaleBackground()` → 复用主游戏 `drawBackground()`（天空+远山+微光），解决"单一"。
- 地面：平涂绿 `#3a7d34` 改为草地渐变 `#4a9e3f→#1f5418` + 顶部高光 `#6cc24a`。
- 新增弹弓：起点固定画两叉+立柱（棕 `#7a4a1e`），拉弓时画两根皮筋 `#3a2410` 连到球，使抬升后的球视觉落于弹弓上。

### 注意
- 主游戏背景 `drawBackground()` 为程序绘制（无图片文件，images/ 下无 bg 图）；dlsq 直接调用即可，无外部依赖。
- `drawBackground()` 只依赖 ctx/screen*/Date，安全可在内嵌小游戏内调用。
- node --check 双文件通过；game.js 由副本 cp 同步。commit 8be7835，tag v1.0.37，**未推送**（等用户指令）。

## 修 bug：大力射手弹弓随球移动（v1.0.38）
用户反馈：刚加的弹弓会跟着球一起移动，不需要跟着动。

### 根因
弹弓绘制用 `sx = g.startBall.x`（固定屏幕坐标，忽略 camX）。飞行时镜头跟随球（`camX = max(0, ball.x - 0.45*boardW)` 使球停在屏幕固定处），弹弓也停在原地、紧贴球，看起来像"跟着球一起移动"。

### 修复
- `sx` 改为 `g.startBall.x - camX`（与地面/km 标记一致的世界坐标）。飞行时弹弓随地面一起向左滚出屏幕，球因镜头跟随留在原地，二者明显分离。
- aim/拖动时 camX=0，弹弓与球同位于起点、皮筋正常连接，行为不变。
- node --check 双文件通过；game.js 由副本 cp 同步。commit c27df1c，tag v1.0.38，**未推送**（等用户指令）。

## 环境坑（重要）：GitHub 推送需代理/VPN 开启
- 本机 `C:\Windows\System32\drivers\etc\hosts` 被某代理/VPN 工具写入大量 `127.0.0.1 github*.com`（含 `github.com`）映射。代理/VPN **关闭**时，git 把 github.com 解析到 127.0.0.1、且真实 IP 20.205.243.166:443 也无直连路由 → `git push` 必失败（Connection reset / Could not connect）。
- 之前 v1.0.27~v1.0.35 能推送，是因为那时代理/VPN 开着。
- **结论**：推送前必须先开启访问 GitHub 的代理/VPN（系统代理/TUN 模式）。不要擅自改 hosts（系统文件，且直连本就到不了）。用户已确认「去开启代理/VPN」后重试。

## 修 bug：「其他游戏」界面 4 款游戏拼音首字母归类错误（v1.0.39）
用户反馈：小青蛙过河、一笔画、大力射手、围住神经猫 在「其他游戏」字母索引里归类不对（未按首字拼音）。
- 归类逻辑取 `OTHER_GAMES[].alpha` 字段（约 2871-2883 行）；字母索引与分组均来自该字段。
- 修正：小青蛙过河 首字『小(xiǎo)』X（原误 F）；一笔画『一(yī)』Y（原误 H）；大力射手『大(dà)』D（原误 L）；围住神经猫『围(wéi)』W（原误 N）。其余 9 款经核对拼音首字母均正确，未改。
- 已用 Node 模拟分组排序验证：字母顺序 B D Q R S W X Y Z 0，分组归属正确。
- node --check 双文件通过；game.js 由副本 cp 同步。commit 55af3fa，tag v1.0.39，**未推送**（等用户指令）。

## 修 bug：「其他游戏」界面数字分组(0)排到字母索引最前（v1.0.40）
用户反馈：把 0（2048）排到 A 前面。
- `computeOtherGamesLayout()` 中 groupLetters 排序原把 alpha==='0' 映射为 'zzz'（排 A-Z 之后）；改为映射为 ' '（小于 'A'），使 2048 数字组排到最前。
- 验证：字母顺序现 0 B D Q R S W X Y Z，0 在最前。commit ede6329，tag v1.0.40，**未推送**。

## 修 bug：忍者切水果水果漂太低（v1.0.41）
用户反馈：水果漂的范围太低，基本都在下半部分；应主要在中部、能到上半屏。
- 根因：vy 写死为 -(760+random*260)（最大约 -1020px/s），重力 G=1500，水果最多上升约 347px，峰值仅略低于屏幕中线。
- 修复：vy 按屏高动态计算，目标峰值落在屏幕 15%~40%（上半部分）：`vy = -sqrt(2*G*(spawnY - peakY))`，spawnY=screenHeight+r+10。
- 验证：568~932 各屏高下水果峰值 y 全部位于上半屏阈值之上。commit f952ba4，tag v1.0.41，**未推送**。

## 优化：忍者切水果背景复用主游戏（v1.0.42）
用户反馈：忍者切水果的背景也需要换成主游戏的。
- `drawMiniGameQiexigua()` 原背景为 `drawRoyaleBackground()`（单调蓝渐变），现改为复用主游戏战场背景 `drawBackground()`（夜空+远山+微光），与大力射手(dlsq)一致。
- drawRoyaleBackground() 调用次数由 13 减为 12。node --check 双文件通过；game.js 由副本 cp 同步。commit 3bcb5bb，tag v1.0.42，**未推送**。

## 优化：数钱数到手抽筋使用原游戏红色钞票图（v1.0.43）
用户反馈：当前绿色方块与原游戏差别太大；需要原游戏的红色钞票图，且钞票放大、靠下一些。
- 从原游戏 `games/sqsdscj/img/m0.png` 拷贝红色 100 元钞票图到 `images/sqsdscj_money.png`（P 模式转 RGBA）。
- 新增 `sqsdMoneyImg` 加载逻辑（仿 qmxz 图片模式），`gSqsdInit()` 中触发加载。
- `drawMiniGameSqsd()` 中用 `drawImage` 绘制钞票替代绿色方块：宽度最大 240px / 棋盘宽 72%，按 371×759 原始比例缩放，限制高度 ≤ 棋盘高 78%；垂直中心从 0.5*boardH 下移到 0.58*boardH。
- 身份文字与 +100 飘字位置随钞票区域自适应；加载前保留绿色卡片 fallback。
- node --check 双文件通过；game.js 由副本 cp 同步。commit d78dfa6，tag v1.0.43，**未推送**（等用户指令）。

## 优化：数钱数到手抽筋 +100 打击特效升级（v1.0.44）
用户反馈：点击钞票的「+100」缺刺激性，需酷炫动效勾起兴奋感。
- 将原普通白字「+100」替换为金色「¥+100」弹跳放大（0.3→1.5→1.0 缩放回弹）+ 金色发光描边 + 黄橙渐变填充。
- 每次点击额外飞溅 7 枚金色「¥」金币粒子（初速度+重力下落+旋转+淡出）。
- 飘字与粒子出现在玩家实际点击位置，强化打击感。新增 gSqsdSpawnHit()/gSqsdUpdate()/drawMiniGameSqsd() 渲染；移除旧 msg/msgT 系统。commit 6330a02，tag v1.0.44，**未推送**。

## 优化：数钱数到手抽筋 点击钞票缩放抖动（v1.0.45）
用户反馈：数字效果不错，希望点击钞票时钞票本身也有抖动/缩放。
- 新增 gSqsd.pulse 字段：点击瞬间 g.pulse=1，gSqsdUpdate 按 dt*5 衰减（约 0.2s 归零）。
- 绘制钞票改为中心原点 translate/rotate/scale：点击瞬间放大至 116%，叠加随时间相位变化的水平抖动(±6px)与轻微旋转抖动(±0.05rad)。
- 与 v1.0.44 的金色爆发/金币飞溅相辅相成，强化点击爽感。node --check 双文件通过；game.js 由副本 cp 同步。commit 见 v1.0.45，tag v1.0.45，**未推送**。
- node --check 双文件通过；game.js 由副本 cp 同步。commit d78dfa6，tag v1.0.43，**未推送**（等用户指令）。

## 推送：v1.0.39~v1.0.45 全部上远端（已成功）
- v1.0.36~v1.0.38 此前已由用户在代理开启后推送。
- v1.0.39~v1.0.45（7 个版本+标签）原先因 GitHub 凭据(GCM 缓存 PAT)失效无法推送；本次用户提供一个临时 Classic PAT（ghp_ 开头），用内联 URL `git push https://ghp_***@github.com/firecser/zombie-hunter.git main --tags` 推送成功：
  - main：`40d4c46..27619cd`，远端 main HEAD = 27619cd（与本地一致）。
  - 标签：v1.0.39~v1.0.45 全部新建并确认存在于远端。
- **PAT 仅用于本次命令内联 URL，未写入 git config、未在本地留存**（用完即弃，不保存明文）。
- **注意**：GCM 中缓存的旧凭据仍可能失效，下次推送大概率仍需重新鉴权（用户提供新 PAT，或在本机带界面终端手动 `git push` 让 GCM 弹登录框刷新）。
- **微信平台上传约定（重要）**：上传前需右键 `game.js` → 全部加固（高混淆+水印）；上传完成后把 `game - 副本.js` 拷回 `game.js` 恢复可读源码。仓库永远提交可读 game.js。

## 优化：我要飞的更高(feidegenggao) 背景复用主游戏（v1.0.46）
用户要求：继续内嵌小游戏优化，「我要飞的更高」要加上主游戏的背景。
- `drawMiniGameFeidegenggao()` 首行 `drawRoyaleBackground();` → `drawBackground();`（夜空+远山+微光），与 dlsq(q1.0.37)、qiexigua(v1.0.42) 一致。
- `drawBackground()` 仅依赖 ctx/screen*/Date，内嵌小游戏内安全；board 区未单独填色，主背景透出，平台/角色绘于其上。
- node --check 双文件通过；game.js 由副本 cp 同步。commit 8b2611f，tag v1.0.46，**未推送**（等用户决定上传/提审）。
- 备注：仍有部分内嵌小游戏用 `drawRoyaleBackground()`（单调蓝渐变），如后续统一风格可继续切换。

## 优化：内嵌小游戏背景统一为主游戏（v1.0.47）
用户要求：把其余内嵌小游戏的蓝渐变背景也换成主游戏背景。
- 全量排查 `drawRoyaleBackground();` 调用：共 11 处。其中 `drawMainMenu()`(3213) 与 `drawOtherGamesPage()`(5858) 属 UI 壳层（非小游戏），按范围不动。
- 其余 9 个 `drawMiniGame*` 的首屏全屏背景由 `drawRoyaleBackground()` 改为 `drawBackground()`：2048、qmxz、bdsjm、bunengsi、zqyl、qingwa、sqsd、sjmao、ybh。
  - 注意：zqyl/qingwa/sqsd/sjmao/ybh 的 `drawRoyaleBackground()` 不在函数首行，而在 `const L/btnTop/lastTick/update` 之后、按钮与内容绘制之前——经逐函数读码确认均为全屏背景调用（非遮罩内重绘），可安全替换。
  - 2048/qmxz 上方原有注释 `// 皇室战争风深蓝背景` 一并改为 `// 主游戏战场背景（夜空+远山+微光）`。
- 替换后全仓库仅剩 2 处 `drawRoyaleBackground()`（主菜单+其他游戏列表页）。
- node --check 双文件通过；game.js 由副本 cp 同步。commit 3a66d26，tag v1.0.47，**未推送**（等用户决定上传/提审）。

## 排查：小青蛙过河(qingwa) 是否无解（结论：有解，非 bug）
用户反馈「小青蛙过河过不去，有没有解」。
- 机制：经典青蛙跳谜题，7 格 `['L','L','L',0,'R','R','R']`，L 只向右、R 只向左，可走一步进空格或跳对方一只落空格；目标 `['R','R','R',0,'L','L','L']`。
- 用 Node BFS 按游戏内 `gQingwaTap/gQingwaHasMove` 的**完全一致规则**求解：确认**有解，最少 15 步**，无逻辑死局 bug。
- 难点：走错顺序会进入「无合法步数」状态，游戏判 `stuck` 需重来 → 对休闲玩家偏难，体感像「过不去」。
- 后续可优化方向（待用户决定）：①减为 2+2+1（更易，8 步）；②加「提示/撤销」按钮；③高亮可动青蛙。代码逻辑本身无需修。

## 重构：小青蛙过河(qingwa) 改三关 + 高亮/撤销/提示（v1.0.48）
用户要求：把小青蛙过河改为三关——第1关=原优化方案①（2左2右1空，5格/最少8步），第2关=当前关（3左3右1空，7格/最少15步），第3关提升难度（4左4右1空，9格/最少24步）；同时加入「撤销」+「提示」按钮，以及常驻高亮可动青蛙。

### 改动
- **关卡系统**：`QW_LEVELS=[2,3,4]` 定义每关每边青蛙数；`gQingwaBuildSlots(n)/gQingwaGoal(n)` 按 n 生成起止局面；`gQingwa.level` 记录当前关。`gQingwaInit(level)` 支持指定关（默认沿用当前关，进入小游戏时 `gQingwaInit(1)` 从第1关开始，通关后 `gQingwaInit(level+1)` 进下一关）。
- **胜利判定通用化**：`gQingwaWinCheck()` 改为按 `gQingwa.n` 校验前 n 个为 R、中间 0、后 n 个为 L（原硬编码 3）。
- **删除全局 `QW_SLOTS=7`**，所有遍历改用 `gQingwa.slots.length` 动态长度。
- **常驻高亮可动（优化③）**：`gQingwaCanMoveTarget(i)` 返回可落点；绘制时**可动青蛙=绿色+金色发光描边**，**不可动=灰色半透明**（一眼区分）。
- **撤销按钮（优化②）**：底部新增 `undoBtn`（居中，与 hint 并列），`gQingwa.history` 历史栈，每步动画完成时压入 `{slots,moves}`；`gQingwaUndo()` 退回一步并清 win/stuck。卡死(stuck)时也可用撤销从死局退回。
- **提示按钮（优化②）**：底部新增 `hintBtn`；点击运行 `gQingwaSolve()`（BFS 求当前局面到目标的最少步解），取**第一步** `hintFrom/hintTo` 高亮 2.2 秒——被提示青蛙金色脉冲发光、目标空格金色脉冲光环。`gQingwaSolve()` 状态空间小（≤9 格）开销可忽略。
- **通关流程**：win 遮罩内居中按钮——第1/2关显示「下一关 ›」、第3关显示「🏆 三关全部通关！+↻ 从第一关重玩」；底部返回/重玩/撤销/提示按钮在 win/stuck 时仍可用。
- **最佳成绩分关存储**：`gQingwaBest` 由单值改为数组 `[0,0,0]`（各关最佳步数），`wx.setStorageSync('gQingwaBest', JSON.stringify(...))`；`getMiniGameBest('qingwa')` 改为返回当前关最佳（modal 显示用）。
- 底部按钮布局在 375×667 验证：back(15-85)/undo(112.5-176.5)/hint(184.5-248.5)/restart(276-360) 互不重叠。
- 三关 BFS 验证均可解（8/15/24 步）。node --check 双文件通过；game.js 由副本 cp 同步。commit e2b375c，tag v1.0.48，**未推送**（领先 origin/main 14）。

## 修 bug：最强眼力(zqyl) 玩法不清晰——缺放硬币环节 + 杯子异色暴露答案（v1.0.49）
用户反馈：开局没有「放硬币」的过程；但杯子又是三种不同颜色，若有放硬币过程又会直接暴露是哪只杯子——玩不明白。
- **根因**：原实现用 `colors=['#4cc9f0','#f72585','#ffd166']` 三色杯子且直接进 shuffle，既没展示金币起点，又因异色可颜色锁定（加了展示=直接暴露答案）。
- **修法（经典三杯猜球正确玩法）**：
  1. 三只杯子改为**外观完全一致**（`cupColor='#c97b4a'`，消除颜色锁定，必须靠追踪移动来猜）。
  2. 新增 `state='show'` 开局部展示阶段：抬起藏币的杯子露出金币约 1.4s（抬升包络 0→72→0px），提示「记住金币藏在哪只杯子下！」；结束自动进入 shuffle。仅首关(init)展示一次，后续关卡不再重揭（否则等于每关送答案）。
  3. shuffle 文案改为「盯紧杯子怎么移动…」强调轨迹追踪；guess/reveal/over 不变。
- 金币绘制统一：`state==='show'||'reveal'` 均绘制 `coinCup`（cups[coinSlot] 对象，hasCoin 挂对象上，洗牌后仍按对象定位，正确）。
- node --check 双文件通过；game.js 由副本 cp 同步。commit 5e534aa，tag v1.0.49，**未推送**（领先 origin/main 15）。

## 优化：最强眼力(zqyl) 洗牌难度——增中间转向次数 + 提速（v1.0.50）
用户反馈：装币杯子基本端到端平移、不在中间掉头，难度低；要求除逐渐提速外，逐渐增加中间位置的转向次数。
- **根因**：原 `gZqylBuildShuffle()` 用 `a=random*(ZQYL_CUPS-1)` 且 `if(a===prev)a=(a+1)%(ZQYL_CUPS-1)`，因 ZQYL_CUPS-1=2 强制 (0,1)↔(1,2) 严格交替，使硬币杯几乎永不在中间掉头。
- **修法**：新增 `gZqylSimulateTurns(swaps,startSlot)` 按游戏规则模拟硬币杯轨迹、统计中间位置(slot1)掉头次数；`gZqylBuildShuffle()` 改为自由随机生成相邻交换对，模拟达标(≥目标)即采用，否则重试至 300 次取最优。`startSlot` 取硬币杯**当前** `.slot`（而非初始 coinSlot），保证模拟与实际动画轨迹一致。
- **难度曲线（已 Node 验证各关 0 失败）**：洗牌步数 `n = 7 + level*2`；目标中间转向 `Math.min(6, level)`（L1→1 … L6+→6 封顶）。移动单步时长 `dur = Math.max(0.12, 0.5 - level*0.03)`（原 0.42-level*0.02），提速更明显。
- node --check 双文件通过；game.js 由副本 cp 同步。commit a02414f，tag v1.0.50，**未推送**（领先 origin/main 16）。

## 优化：最强眼力(zqyl) 命数显示——矢量心形红→白（v1.0.51）

用户反馈：点错扣命，但右上角命数一直显示白色（原用 emoji `❤`/`🤍`，Canvas 里 emoji 不随 fillStyle 变色、设备只渲染成白色）。
- **修法**：弃用 emoji，新增 `drawZqylHeart(cx,cy,s,active)` 用 bezier 矢量绘制心形——`active` 时填 `#ff3b3b`+浅红描边（红），非 active 时填半透白+白色描边（白）。命数循环 `i<gZqyl.lives` 则红，否则白；初始 lives=3 全红，扣命后从右往左逐个变白，lives=0 全白且 `gZqylUpdate` 已置 `state='over'` 游戏结束（逻辑未动）。
- node --check 双文件通过；game.js 由副本 cp 同步。commit c6b7ac6，tag v1.0.51。
- **推送（用户指令「推送给远端」）**：用 MEMORY.md 内联 PAT 推 `main` + `--tags` 至 github.com/firecser/zombie-hunter.git，成功。`27619cd..c6b7ac6` main 推进，新建标签 v1.0.46~v1.0.51（共 6 个）。PAT 仅用于本次内联 URL，未写入 git config、未进仓库。本地 `origin/main` 跟踪分支需 `git fetch` 才刷新（status 暂显 ahead 17 为旧值）。

