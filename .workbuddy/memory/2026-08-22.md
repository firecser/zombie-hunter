# 2026-08-22 工作日记

## 关卡数据全量填充（10章60关）— 完成并提交
- 提交 `3716aed`（game.js）+ `d7ba712`（同步镜像 game copy.js），工作树干净。
- 背景：原 `STAGES` 仅 6 条，但 `currentStage` 取关卡号 1–60，`getCurrentStage()` 用 `STAGES[currentStage-1]`，导致 7–60 关全部钳到 STAGES[5]（潜在 bug）。

### 关键改动（game.js）
1. **CHAPTER_THEMES + buildStages()**：替代 6 条 STAGES，程序生成 60 关（10章×6关）。
   - 五行单系章节：Ch1 normal(教学) / Ch2 水(克→土) / Ch3 木(克→金) / Ch4 火(克→水) / Ch5 金(克→火) / Ch6 土(克→木)。
   - 双系相生章节：Ch7 金+水 / Ch8 火+土 / Ch9 木+火 / Ch10 水+木（取 相生 相邻对）。
   - 难度锯齿：章内 `×(1+0.03*(lvInCh-1))`，章首喘息 ×0.85、章末 Boss 高潮 ×1.15；`chapterMult=1.16^(ch-1)`；并与天赋解锁章(2/4/6/8/10)交替上升。校验确认每个章界难度回落。
2. **出怪接线 (#266)**：`spawnWave` 读取 `stage.elements`（双系数组，单系退化取 `[stage.element]`），`updatePendingSpawns` 写 `zombie.elements`/`zombie.creature`/`bossCreature`（boss 用 `{creature}King`）。
3. **getElementBonus 双系 (#270)**：`zombie.elements.length>1` 时逐元素算加成取最坏(min)，单系路径不变（克制 30%+压制 30% / 被克 30%−30% / 无关仅 30%）。
4. **极地怪物皮肤 (#269)**：`CREATURE_VISUAL` 调色板 + 每物种特征（耳/獠牙/喙/鳍/翼/尾/王冠光环）；20 种：海豹/企鹅/北极熊/海象/北极狐/驯鹿/虎鲸/雪鸮/海鹦/冰龙 + 各 boss（King）。保留冰冻/减速覆盖层。
5. **UI 可见性**：选关卡片加五行属性徽标；战斗 HUD 右上角加章节属性点（克制提示一目了然）。
6. **getCurrentStage 加固**：越界时 clamp 到首尾。

### 校验
- `node --check` OK。
- 头less 烟测（mock wx/canvas）：60 关 elements 全对（normal→水→木→火→金→土→金+水→火+土→木+火→水+木）、克制/双系最差值数学正确、20 皮肤+boss 渲染不崩、难度每章界回落、出怪 wiring 正确（普通僵尸 elements=['金','水']+creature=orca，boss=orcaKing）。
- 临时 `_smoke.js` 跑完即删。

## 金币经济重算（获取直线 + 消耗二次幂）— 完成并提交
- 提交 `3a1afbf`（game.js）+ `061beb2`（镜像），工作树干净。
- 背景：原获取来自击杀掉落（随怪量波动，非直线）；原消耗 `upgradeTalent` 用 `talent.cost` 作**每级固定**价（直线），不符合二次幂。

### 改动（game.js）
1. **新增经济常量 + 曲线函数**（置于 `upgradeTalent` 之上）：
   - `GOLD_BASE=200`, `GOLD_SLOPE=30` → `getStageGoldReward(s)=200+30*(s-1)`（直线：第1关200、第60关1970，斜率恒30）。
   - `COST_QUAD_SCALE=4` → `getTalentCost(t)=round(t.cost*(L+1)²/4)`（二次幂：早期廉价、后期陡峭）。
2. **消耗接线**：`upgradeTalent`、`drawTalentModal`（按钮可购判定 + 两处 🪙 显示）、`handleTalentModalClick` 全部改读 `getTalentCost(talent)`；`talent.cost` 保留为二次幂 BASE。
3. **获取接线**：`victory()` 累加 `getStageGoldReward(currentStage)`（失败 gameOver 不发奖励，保持失败无收入）。
4. **压低击杀金币**（`zombieTypes` gold 5/8/15/50 → 2/3/5/15），使直线通关奖励成为金币获取主导，获取曲线真正平直。

### 校验
- `node --check` OK。
- 头less 烟测：直线 slope=30（r1=200/r60=1970）；二次幂样本 75/300/675/1200/1875 = 300×(1²..5²)/4，比值 4.000 / 9.000 精确对应 (L+1)²。临时 `_smoke2.js` 删。
- 体量参考：damage(base300,max30) 满级累计 ≈ 300/4×Σ1²..30² ≈ 709k；deathray(base5000,max5) ≈ 69k。线性一次满通 ≈ 65k，配合刷关形成「前期收入盖过消耗、高阶被二次幂卡住」的自然门槛。

## 结算弹窗突出金币 + 平衡微调 — 完成并提交
- 提交 `a46081e`（game.js）+ `b68a8f6`（镜像），工作树干净。
- 用户三需求：① 结算弹窗突出「获得多少金币」；② 城墙初始血量 → 3000；③ 炸弹去掉「每5级+1」，只留广告 + 初始引导炸弹。

### 改动（game.js）
1. **城墙血量**：`WALL_MAX_HEALTH` 600 → 3000（startGame 与 player 声明均引用该常量，自动生效；无城墙血量天赋）。
2. **移除每5级加炸弹**：删 `levelUp()` 内 `if(player.level%5===0){bombCount++...}` 整块。剩余炸弹来源仅两处：广告 `grantAdBombAndRelease`（bombCount+=1，约 line 1883）、买量演示引导 `startGame` 内 `if(isAdDemoMode){bombCount=1}`（约 line 6176）。与「只保留广告和初始引导」一致。`justGotBomb`/`bombFull` 标志不再被升级置位（升级面板不再弹炸弹提示）。
3. **结算弹窗突出金币**：
   - 新增全局 `stageGoldEarned`，在 `victory()`（=击杀+通关奖励）与 `gameOver()`（=仅击杀）结算时赋值。
   - 新增 `drawGoldBanner(x,y,w,h,amount)`：金色渐变横幅 + 「🪙 本次获得金币」+ 大号数字。
   - `drawGameOver`（modalH 220→248）加入横幅；`drawVictory`（250→268）替换原小字「获得金币」为同款横幅，按钮统一为正方形（修复原绘制48方 vs 点击68×30不一致的历史错位）。
   - **点击命中区同步**：`onTouchStart` 中 gameOver/victory 分支的 modalH/btnSize/btnY/gap 与绘制完全一致；victory 改为按索引分发（下一关/重玩/关卡，全通关时仅重玩+关卡）。

### 校验
- `node --check` OK。
- 头less 烟测（`_smoke3.js`，vm + mock wx/canvas）：WALL_MAX_HEALTH=3000 ✓；startGame 后 health/maxHealth=3000 ✓；连续升级到20级 bombCount 不变 ✓；`getStageGoldReward(3)=260` ✓；victory 后 stageGoldEarned=460（1000起+200杀+260奖励）✓；gameOver 后 =200 ✓。临时 `_smoke3.js` 删。

### 注意
- 无头加载需在 wx stub 加 `createImage`（顶部 `loadOtherGameIcons` 调 `createGameImage`→`wx.createImage`）。
- 本次仅本地提交，未打 tag/推送。

## 技能升级平衡（v1.1.59）— 完成并提交
- 需求：局内技能升级效果太弱。单纯加攻击就加高；出现增加目标/扩大范围则相应减攻击，维持「纯伤 > 覆盖」阶梯模型。
- 提交 `ba443d0`（game.js）+ `0c8346e`（同步镜像 game copy.js）。
- 改动（集中在 4 个常量 + SKILL_DEFS 分支 + shoot* 落地）：
  - `MULTI_BULLET_DMG_PENALTY=0.20`：多子弹每多 1 发，单发 ×1/(1+0.20·k)。
  - `REACH_DMG_PENALTY=0.20`：穿透+1 → 单发 ×1/(1+0.20)（shootAttribute/shootWood）；弹射目标+1(闪电链) / 冻结扩大范围 → ×(1-0.20)^k。
  - `PURE_DMG_BASE=1.25`：基础「火力强化」每级（原 1.14）。
  - `PURE_DMG_BRANCH=1.40`：纯伤分支每级（重型枪管/富燃料/高压电弧/裂地穿刺；原 ~1.20）。
  - freeze 分支 glacier/frostNova 扩大范围时 `dmgMul *= (1-0.20)^bl`，默认 dmgMul:1（attributeMods 与 recomputeFreezeMods 两处）。
- 关键修复：原把常量写在 SKILL_DEFS(1065) 之后(1415)，而 desc 字符串在对象字面量求值期引用它们 → TDZ 崩溃（微信也会加载失败）。已将 4 常量上移到 SKILL_DEFS 之前。
- 验证：无头烟测（`_smoke.js`，vm+stub wx/canvas）跑通 18 项断言 + 单帧 gameLoop 不崩，临时文件已删。
- 本次仅本地提交，未打 tag/推送。

## v1.1.60 平衡微调 — 完成并提交（本地，未推送）
- 提交 `bcd6ced`（game.js）+ `a232246`（同步镜像 game copy.js）。
- 改动：
  1. 移除初始赠送炸弹：`startGame` 买量演示分支原 `bombCount = 1;`（line 6190）删除；炸弹现仅通过看广告(+=1)获得。买量引导改为提示看广告补弹，无弹时点按钮仍走广告流程，不会卡死。同步更新 line 5837 注释。
  2. 纯伤继续加强：`PURE_DMG_BASE` 1.25→1.5、`PURE_DMG_BRANCH` 1.40→1.7，维持「分支>基础」阶梯关系（重型枪管/富燃料/高压电弧/裂地穿刺均走 BRANCH）。charge 分支(1.55)未动（射速-13%/级的折中项，介于基础与纯伤分支之间合理）。
- 验证：无头烟测跑通 8 项（常量值、分支 effect、初始炸弹已移除、单帧 gameLoop 不崩），临时 `_smoke.js` 删。
- 用户确认此前对「+80%」理解为“显著加强纯伤”而非字面每级+80%（避免后期爆炸）。本次仅本地提交，未打 tag/推送。

## v1.1.61 怪物数量/血量再平衡 — 完成并提交（本地，未推送）
- 提交 `d3a108e`（game.js）+ `f5bfbcd`（镜像）。
- 背景：用户要求"怪物数量和血量重新调整，适配战斗中玩家能力增长（含攻击对象数量变化）"。上轮已把纯伤回退 1.25/1.40（任务284）。
- 方法：写无头真实战斗模拟器 `_balance.js`（vm+stub wx/canvas，自动选卡=懂五行克制+堆火力/覆盖，自动开火），多关跑3次取胜率，用数据驱动而非拍脑袋。修复了密度逻辑 const 重赋值 bug（`comp` 改 push 扩展）、补上 exp/DENSITY 让总经验不变。
- 改动：
  1. 纯伤回退：PURE_DMG_BASE 1.5→1.25、PURE_DMG_BRANCH 1.7→1.40（用户"还是1.25好点"）。
  2. 怪物密度 MONSTER_DENSITY=1.5（buildWavePlan 每只怪 50% 概率复制；updatePendingSpawns 里 exp 除以 DENSITY，总经验不变→升级节奏不变）。
  3. 墙血 WALL_MAX_HEALTH 3000→5000；啄墙系数 0.03→0.015（给多目标玩家清场窗口）。
  4. STAGES 关卡间曲线 base 1.12^ch→1.03^ch（压平，匹配每关从 Lv1 重置的玩家）。
  5. WAVE_TIER_TABLE 关内 hpT 1.0→4.2 压到 1.0→2.10，dmgT 1.0→2.4 压到 1.0→1.48（匹配玩家 Lv20 顶峰 ×1.25^19≈73倍全局伤害）。
- 验证结果（模拟器，合格多目标玩家）：关卡1-29 稳过（墙血50-83%）；章末/高章（30/45）险过（墙血17-25%，胜率1-2/3）；全60关均可过，前期平滑适配成长、后期有真实压力。确认"多目标"维度已纳入（密度×1.5 提供更多可击目标，覆盖类减伤−20%仍生效）。
- 模拟器 `_balance.js` 临时文件已删。未打 tag/推送。

## v1.1.62 出怪节奏（对症"大量怪同时段"）— 完成并提交（本地，未推送）
- 提交 `67809c8`（game.js）+ `a976fd6`（镜像）。
- 用户反馈："战斗难度大是因为前期怪物数量太多，一杆枪打不过大量怪几乎同时段过来；别改血量/伤害。"
- 根因定位（非血量）：出怪时间轴 buildWavePlan 第二遍原把整局塞进 GAME_TIME_LIMIT，且波与波首尾相接无空当；density×1.5 后 totalCount 更大→间隔更小→同屏峰值更高。外加买量演示 spawnInitialAdZombies 一次性 push 15-20 只到场上（瞬间同屏20）。
- 改动：
  1. 新增常量 WAVE_SPAWN_GAP=900（每只怪最小间隔下限）、WAVE_GAP=1200（波间缓冲空当）。WAVE_INTERVAL 取 max(WAVE_SPAWN_GAP, 原估算)，波末加 WAVE_GAP。让玩家逐批清而非被数量淹没。
  2. spawnInitialAdZombies 改为经 pendingSpawns 错峰入场（at=1500+i*900），updatePendingSpawns 对带 _adTemp 的怪用初始坐标+慢速，并标 isAdZombie。修了一处残留旧 zombies.push 属性导致的语法错。
  3. 血量/伤害/墙血/密度完全未动（回应"别改血量伤害"）。
- 验证：可靠逐秒采样器（_dbgN，o[0] 选卡）确认关卡1-5 同屏峰值仅 30-42 只、墙血缓降不崩；正常关因间隔放大同屏峰值显著下降。注：另一模拟器 _bal2 的"峰值202/关卡1崩"是其 pick 选卡策略的模拟器副作用（upgrade 态堆积），非真实游戏——已用 _dbgN 交叉验证真实前期不密。
- 临时文件 _bal2/_dbg1/_dbgN 已删。未打 tag/推送。

## v1.1.63 去掉买量演示初始怪 — 已打 tag 推送
- 提交 `4c2a34e`（game.js）+ `6df6ec0`（同步镜像 game copy.js）。annotated tag v1.1.63 已推送远端。
- 改动：删除 startGame 中 `spawnInitialAdZombies()` 调用（adDemoMode 块内）及整个 `spawnInitialAdZombies()` 函数定义；玩家正常从首波起手（买量演示不再开局瞬间同屏 15-20 只）。
- 验证：无头 ad demo 模式 startGame 后 zombies=0、pendingSpawns=0。

## v1.1.64 留存改造 — 已提交（本地，未打 tag/推送）
- 提交 `c3fd0f3`（game.js，+401行）+ `fdd9b87`（同步镜像 game copy.js）。
- 背景：诊断玩家单日<30min（体力硬卡 100/20=5局×5min=25min + 恢复5min/点过慢）+ 次日留存0（代码无签到/推送/每日任务/每日登录奖励）。用户明确 **不做 P0 时长天花板改动**，其余全部做。
- 落地 6 项：
  1. **#288 章节动态解锁**：新增 `isChapterUnlocked(chapter)`（第1章恒开；其余章首关 `chapter.levels[0]` 需 `stageProgress[首关-2]` 为真），replace_all 把 6 处 `chapter.unlocked` 改 `isChapterUnlocked(chapter)`。开放 L2-L60。
  2. **#289 每日签到**：`lastSignInDate/signStreak/signTodayClaimed/signModal`，`SIGN_REWARDS`（7天：100+20 / 150+20 / 200+30 / 250+30 / 300+40 / 400+40 / 500+50 金/体），`checkDailySignIn()`（首登弹窗、断签 streak 归零）、`claimDailySignIn()`（连签+奖励+存档）。drawSignInModal 7格+领取钮。
  3. **#290 订阅消息召回**：`RESUBSCRIBE_TEMPLATE_IDS`（留 TODO 占位，待微信后台配模板）、`requestRecallSubscribe()`（每日限一次），victory/gameOver 末尾调用。
  4. **#291 每日任务**：`DAILY_TASKS`（clear×3→150金20体 / kill×100→100金10体 / ad×1→80金10体），进度 dtClear/dtKill/dtAd，addDailyTaskProgress 接 victory('clear')+击杀点('kill')+showRewardedAd onReward('ad')；drawDailyTaskModal + 右下角浮动入口按钮（可领红点）。
  5. **#292 失败金币重试 + 次日回归礼**：`GOLD_RETRY_COST=50`，gameOver 重玩钮改 💰50，不足 toast；`isReturningPlayer`（有历史登录且昨未登）在 claimDailySignIn 内额外 +100金 +30体，弹窗标题"欢迎回来"。
- 坑：① 曾误用 `addSkillPoint`（游戏无技能点系统，已删 skillPoint 字段，签到只用金币+体力）；② 无头 stub 逐步补 wx.onTouchStart/ctx.createLinearGradient/requestAnimationFrame/AudioContext。
- 验证：语法 PARSE OK；无头确认首登弹签到窗、连签+金+体、通关L1-6→第2章解锁true/第3章false、clear×3+kill100+ad→全完成领奖+330金。
- **待办**：RESUBSCRIBE_TEMPLATE_IDS 需用户在微信后台建订阅模板填实际 ID；是否打 tag v1.1.64 并推远端待用户确认。

## v1.1.64-fix 签到/任务弹窗拦截修复 — 已提交（本地，未推送）
- 提交 `7784ae4`（game.js，+10行）+ `e8ad679`（同步镜像 game copy.js）。
- **Bug**：用户在「关卡/天赋/排行榜」Tab 时签到弹窗挡住屏幕中央，点击"领取今日奖励"按钮无反应；切到「主角」Tab 后点击才生效。
- **根因**：`onTouchEnd` 内对关卡/天赋/排行榜 Tab 的点击处理走独立分支（line 14626/14634/14649），仅 `mainMenuTab === 'hero'` 时才调 `handleMainMenuTouch`（line 14674）；而 `handleMainMenuTouch` 是 `handleSignInClick` 的唯一入口。弹窗显示时其它 Tab 的点击**穿透**——既不调 handleSignInClick 也不 return。
- **修复**：在 `onTouchEnd` miniGame 分支之后、Tab 点击分支之前加顶层拦截——`if (gameState === 'mainMenu' && signModal.show) { handleSignInClick(endX, endY); return; }` + dailyTaskModal 同样处理。**不依赖 mainMenuTab**，所有 Tab 都能关闭弹窗。onTouchStart 不拦截（保留底部 tab 切换能力，对应用户原描述"tab能切换"）。
- **验证**：无头回归 4 个场景（level/hero/talent/rank Tab + 弹窗）——修复前 level Tab 不响应，修复后 4 个场景全部正常：弹窗关闭、streak+1、+100金币、存档生效。
- 底部 Tab 切换行为不变（仍在 onTouchStart line 14041-14062）。

## v1.1.64-fix2 每日任务弹窗交互修复 — 已提交（本地，未推送）
- 提交 `677fa98`（game.js，+70行-8行）+ `e7c964e`（同步镜像 game copy.js）。
- **用户反馈**：① 弹窗关不掉；② 点击"未完成"按钮没反应；③ 未完成为什么是个按钮（UX 困惑）。
- **根因**：原 `drawDailyTaskModal` 把"未完成"用 `drawRoyaleBevelButton(..., 'blue')` 画成蓝色按钮，误导可点；`handleDailyTaskClick` 仅 `dailyTaskClaimed` 时关弹窗，点遮罩外/任务行返回 false 不关；领取后也不关。
- **修复**：
  1. 加关闭按钮（右上角 ×，`dailyTaskCloseBtn`），点 × 关闭。
  2. 点弹窗遮罩外部（modal 矩形之外）关闭弹窗。
  3. "未完成"改为灰色静态块（"完成任务后领取"，不可点）；"已领取"改为绿色静态"✓ 今日已领取"（也不可点，点任意处关）。
  4. 领取按钮仅"全部完成且未领取"时为金色可点；领取成功后 `dailyTaskModal.show=false` 自动关闭。
  5. 任务行右侧加 `p/target` 进度计数（原只在 desc 文本内含进度）。
- **验证**：无头 5 场景——未完成点灰块不关(modal保持true)、点×关、点遮罩外关、完成领取+330金并自动关、已领取点任意处关，全部符合。
- 注意：每日任务浮动入口按钮 `dailyTaskEntryBtn` 仅 `mainMenuTab==='hero'` 时经 handleMainMenuTouch 生效（其它 Tab 不调 handleMainMenuTouch），与签到弹窗同病；本次未处理（非当前 bug），后续可考虑在 onTouchEnd 顶层也加入口按钮拦截。

## v1.1.64-fix3 每日任务入口按钮拦截修复 — 已提交（本地，未推送）
- 提交 `3f5cfea`（game.js，+8行）+ `712a771`（同步镜像 game copy.js）。
- **用户反馈**："发现了bug就修"——指之前 fix2 备注里提到的"每日任务浮动入口按钮仅 hero Tab 生效"问题。
- **根因**：`dailyTaskEntryBtn` 的点击处理只写在 `handleMainMenuTouch` 内（line ~13836），而该函数**仅 `mainMenuTab === 'hero'` 时**在 onTouchEnd 被调用；关卡/天赋/排行榜 Tab 根本不调它 → 入口按钮打不开弹窗。
- **修复**：在 `onTouchEnd` 弹窗拦截之后、内嵌小游戏之前加顶层拦截（与签到/任务弹窗同级，不依赖 mainMenuTab）：
  ```js
  if (gameState === 'mainMenu' && !dailyTaskModal.show &&
      endX >= dailyTaskEntryBtn.x && ... && endY >= dailyTaskEntryBtn.y && ...) {
      dailyTaskModal.show = true; return;
  }
  ```
- **验证**：无头 4 Tab（level/talent/rank/hero）点击入口按钮中心(337,564) → dailyTaskModal.show 全部 = true。调试中发现测试里 signModal 未关会先被签到拦截return（真实流程先领签到，属正常）。
- 注意：签到/任务弹窗显示时，入口按钮拦截因 `!dailyTaskModal.show` 前置条件不会误触发（弹窗开着时点击走弹窗拦截分支）。

## v1.1.64-fix4 入口按钮仅主角Tab显示 — 已提交（本地，未推送）
- 提交 `25c9855`（game.js，+5行）+ `03bef47`（同步镜像 game copy.js）。
- **用户要求**："每日任务按钮，只需要出现在主角tab，其他地方不要出现。"
- **修改**：`drawDailyTaskEntryButton()` 开头加 `if (mainMenuTab !== 'hero') { dailyTaskEntryBtn = {0,0,0,0}; return; }`——非主角 Tab 不绘制且清零命中区，避免残留可点区域。
- 注：fix3 已让入口按钮在所有 Tab 可点击，但用户明确只要主角 Tab 显示，故此处收窄。onTouchEnd 顶层拦截保留（命中区清零后不会误触发）。
