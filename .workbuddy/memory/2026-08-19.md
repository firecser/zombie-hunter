# 2026-08-19

## 难度心流重做（9 段波次 + 技能解锁重排）
- 用户反馈：波次只有 3 段太少了、难度不均匀、技能升级跟不上。要求 ≥9 段、结合技能解锁等级提高强度、心流曲线两条线交叉上升、偏离收敛 20% 内。
- 改动（仅 `game.js`，可读版；`game - 副本.js` 未动/冗余）：
  1. `updatePendingSpawns`（~L5516）接入 `getWaveTier(p.wave)`：怪物 HP=STAGES.healthMult×tier.hpT、伤害=STAGES.damageMult×tier.dmgT。废弃旧 `stage.hpWaveGrow/hpGrow` 逻辑。
  2. 技能分支 `reqLevel` 批量重排 2/3/4/5/6 → **5/8/11/14/17**；雷霆一击(单次性) → **Lv18**（脚本 `_remap_req.js` 临时跑，已删）。
  3. `WAVE_TIER_TABLE`（9 段，hpT 1.00→4.20，每步 ×1.10–1.16）此前已存在，本次落实了"真正接入出怪"。
- 心流核算：怪物 tier 跳档(波5/8/11/13/16/18/20) ≈ 技能分支洪流(Lv5/8/11/14/17/18)，升压同步获强。均值玩家(r≈8%)偏差≤23%(玩家领先=安全)；调平衡只需改 `WAVE_TIER_TABLE` 与 `reqLevel` 两处。
- 提交 `9a8c658`（本地，未推送，按用户"不推送"规矩）。
- 注：当前 `game.js` 是用户微信开发者工具去混淆后的可读版（v1.1.33~v1.39 区间，非最新 HEAD；缺 v1.1.53 失败弹窗变强/返回改动）。

## 仓库约定更新（副本文件名 + 同步工作流，最高优先级）
- 用户：删掉旧 `game - 副本.js`，重命名为 `game copy.js` 作为正式副本；加固由用户用微信开发者工具自理，**我绝不混淆代码**。
- 新副本文件名：`game copy.js`（取代旧 `game - 副本.js`）。
- 标准工作流：① 编辑 `game.js`（唯一可读源码）→ ② `git commit` → ③ `cp game.js "game copy.js"` 并一并提交，使二者在仓库内始终一致，防信息丢失。
- 已将旧副本删除、新副本 `game copy.js`（与 game.js 字节一致、0 混淆标记）入库，并更新 MEMORY.md 对应约定段。
- 本次提交（本地，未推送）：删 `game - 副本.js` / 加 `game copy.js` / 更新 MEMORY.md + 本日记。
