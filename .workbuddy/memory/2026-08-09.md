## v1.0.79 后续修复：倍增门结算遮罩盖住按钮
- 问题：drawMiniGameBzsmen 的 gameOver 遮罩用 `fillRect(0,0,screenWidth,screenHeight)` 全屏绘制，且在按钮之后绘制，导致结算后顶部「‹ 返回 / ↻ 重玩」被遮罩盖住、不可见不可点。
- 修复：把两枚按钮的 `drawMiniGameButton` 调用移到 gameOver 遮罩之后（置顶绘制），与 xnf 小游戏的「HUD 始终置顶」写法一致。
- 排查：其他小游戏（qmxzfzm/bdsjm 用"遮罩只盖游戏区、留底部按钮"；xnf 用"按钮置顶"）均无此问题，beishumen 是唯一遗漏。
- 已 cp 同步 game.js，node --check 双文件 OK。未提交 tag（按惯例等用户决定是否上传）。

## 按钮位置统一到底部
- 用户要求倍增门的「‹ 返回 / ↻ 重玩」与其他小游戏一致放在**屏幕底部**（原先在顶部 btnY=16）。
- bzsmenLayout 改为：btnH=32，btnY = screenHeight-16-32（与其他小游戏完全一致的 w84/h32 底部定位）；rowB 与按钮解耦固定为 68（顶部分数框）。
- 赛道面板与高亮带原本延伸到 screenHeight+10（覆盖底部），现收口到 backBtn.y-12（按钮上方），避免门落到按钮区；核心球 coreY 不变（0.34H），仍在面板内。
- 按钮仍置顶绘制（在 gameOver 遮罩之后），结算后可见可点。已 cp 同步、node --check 双文件 OK。

## 小球下移
- 用户反馈小球（核心球）太高。原 coreY = screenHeight*0.34（屏幕靠上 1/3）。
- 重构 bzsmenLayout：赛道面板改为从分数框下（panelTop=rowB+46）一直到按钮上（panelBottom=btnY-12），coreY 改为面板下半区 `panelTop + (panelBottom-panelTop)*0.6`（约屏幕 0.6 高度），玩家预判来门时间更长。
- draw 中面板/高亮带改用 L.panelTop/L.panelBottom（原先从 coreY-30 起算，会导致小球下移后上方留大片无面板空白）。g.coreY 仍由 L.coreY 初始化。
- 已 cp 同步、node --check 双文件 OK；已确认无残留 `coreY-30` 旧引用。

## 倍增门 → 守桥射击 重写（复刻豆包文档 V1.1）
- 用户要求把「倍增门」小游戏改成和豆包那篇「竖版词条肉鸽+守桥防守射击」策划文档一模一样。确认范围：**核心循环 + 第1关**，**功能化复刻**（数字/系统严格按文档 V1.1，画面沿用现有小游戏简洁风格；返回/重玩按钮在底部统一）。
- 用 Python splice 精确替换原 bzsmen* 区块（原 7511–7729 行），重写为 sq* 实现（sqLayout/sqInit/sqUpdate/sqDamageChest/sqBreakChest/sqPickAffix/sqDamageEnemy/sqExplode/sqBreach/sqSpawnWaves/sqSpawnWave/sqApplyFaction/drawMiniGameSq/handleMiniGameSqInput 等）。
- 改 6 处接线：状态变量 `gBzsmen→gSq`；OTHER_GAMES name 改「守桥射击」emoji🛡️（id 沿用 beishumen）；startMiniGame→sqInit；gameLoop draw→drawMiniGameSq；touchEnd→handleMiniGameSqInput；onTouchMove 新增 beishumen 分支做**拖动走位**（旧版被兜底吞掉无法拖动）。
- 已实现文档 V1.1 全系统：初始属性(攻8/隔0.5/弹道3/暴5%/暴伤150%/防线10/护盾3)、5类宝箱(血=200×类型×位置×K，L1+30%)、5类敌人(血=40×系数×K×波增，越线扣血)、4品质6类词条(百分比加法叠加+掉落权重)、金龙→仙道/魔道二选一、循环波封顶(×1.15×5次 / ×1.07≤2倍)。坐标用 1080×1920 比例映射。
- 当前仅启用 SQ_LEVELS[0]（第1关全量）；L2–L5 数据已在文档结构化预留(TODO)，后续按文档接入。冰盾/限时/破甲/爆炸/弹射逻辑已写(L1 无冰盾/限时箱但通用)。
- 验证：node --check 双文件 OK；无头冒烟(stub wx/ctx)跑 900 帧+模拟拖动/碎箱/阵营/拾取/波次/越线/失败，SQ_SMOKE_OK；临时测试文件已清理。本地未提交（HEAD 仍在 v1.0.79 tag，下一版 v1.0.80）。
- 待真机回调：子弹/敌人速度比例、数值手感需实跑微调。
