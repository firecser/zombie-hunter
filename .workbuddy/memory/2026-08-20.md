# 2026-08-20 工作日记

## 推送 v1.1.55（完成）
- `git -c http.sslVerify=false push https://$(cat .pat)@github.com/firecser/zombie-hunter.git main v1.1.55` 实测跑通（代理 MITM 下需关 TLS 校验；`http.schannelCheckRevoke=false` 对该 git 版本无效）。已更新 MEMORY.md 推送命令段。
- main: d198304..d876756 已推；新标签 v1.1.55 已创建。

## 整理 v1.1.33–v1.1.54 变更 + 完成度核对
- 产出：`docs/changelog-1.1.33-1.1.54.md`（按 A 天赋UI / B 分栏子页+深养成 / C 交互修复 / D 金币制+弹窗打磨 / E 失败弹窗 五组整理）。
- **核实结论**：当前可读 `game.js`（14084 行，去混淆早期间构建）实际对应 ≈ v1.1.33–v1.1.39：
  - ✅ 含：天赋树 v4.1–v4.6 竖版 UI、18 节点扁平天赋树(chapter 1/2/4/6/8/10)、金币制升级弹窗、五行技能系统、战斗力、本会话加的 9 段难度。
  - ❌ 缺失：v1.1.40 分栏子页(根脉/五行/统御/防御)、v1.1.41 五行深养成链 I/II/III+统御树(74→119 节点，当前仅 18)、v1.1.53/54 失败弹窗「变强」按钮(drawGameOver 仅 重玩/关卡)。
- 关键证据：`talentData` 仅 18 节点（core…devour）；全文无「根脉/统御」；`drawGameOver` L3770 只有两按钮。
- 含义：区间内功能多数曾提交 git（但 game.js 是混淆产物），不在当前可编辑源码；缺失三簇需在当前可读 base 上重做或恢复更晚去混淆构建。

## 恢复缺失三簇（用户指令「全部补回来」→ 忠实重建）
采用方案：数据驱动忠实重建（用户在 AskUserQuestion 选「忠实重建（推荐）」），不手写 60+ 节点，靠生成器产出并确保保留现有战斗加成字段。

### A/B 簇：分栏子页 + 五行深链 + 统御树 + 防御链（game.js）
- 注入（L6447 起，`buildTalentTree()` 立即执行）：
  - `TALENT_PAGES` = 根脉/五行/统御/防御 四子页；`talentLayout` 每页有序布局（表头 h + 节点 id）。
  - `mkTalent`/`scaleContrib` 生成节点（扁平 talentData，含 `page`/`contrib`）。
  - 五行：金/木/水/火/土 各 I/II/III 三阶(甲/乙) + 共鸣 cap = 5×7=35 节点。
  - 统御：12 节点（`sup_*`），前置指向五行 cap；终极 3 个(deathray/immortal/devour)放基础。
  - 防御：14 节点（`def_*`）。
  - 结果：talentData 共 **79 节点**（root10/element40/supreme15/defense14）。
- `drawMainMenuTalent` 重写为数据驱动：四 Tab（`getTalentTabRects`/`drawTalentTab`），内容区裁剪 + 网格 + `talentScrollY` 滚动 + 前置连线；`talentNodes` 命中坐标已含滚动偏移（修复一坑：原把未滚动 y 存入命中数组）。
- `applyTalentsToBattle` 增加通用 `contrib` 循环（goldMult/expMult/critDamageMult 用乘，其余累加），新节点声明 contrib 才生效，基础 18 节点不受影响。
- 触摸接线：touchStart 设天赋拖动起点；touchMove 加 `isTalentDragging` 滚动；touchEnd 先 `handleTalentTabClick` 命中子页切换（不误触节点），再 `handleTalentClick`/`handleTalentModalClick`；reset 段清 `isTalentDragging`。

### E 簇：失败弹窗「变强」按钮（上一轮已完成）
- `drawGameOver` 三按钮（重玩/变强/返回）；touchEnd 中间按钮 → mainMenu+talent+root；第三个 → level。语法已核验 OK。

### 校验
- `node --check` OK。
- 提取纯数据段校验：79 节点、每节点有 page、所有 prerequisite.id 存在、applyTalentsToBattle 不抛。
- 头less 烟测（mock wx/canvas）：四页分别渲染 10/40/15/14 节点、Tab 点击命中、drawTalentModal/handleTalentModalClick 不崩。
- 修复真实 bug：防御深链 `D()` 建了 talentData 却没 push 进 `talentLayout.defense`（导致防御页 0 节点），已改为 D 内 push。

### 待办（本次未做）
- 提交 game.js + 同步「game copy.js」镜像（按硬性流程：编辑→commit→cp 镜像→commit）；记忆更新后待 push 新 tag（用户后续决定）。
- 可选：`loadPlayerData` 仅回填已存在 key，生成节点保持默认，重载安全（已确认）。

## 第三次改动：DAG 拓扑布局重构 drawMainMenuTalent
- 用户提反馈："挪动天赋图标，重新布局，让有前置关系的天赋都能直接相连，而它们之间的连线不要穿过其他天赋图标"（拓扑图）。
- 新增 `layoutTalentPage(pageId, contentTop, contentW, nodeSizeParam, xGap, yGap)`：
  - 1. 拓扑分层：每节点 `layer = 父 layer + 1`，前置不在本页→L0（避免跨页边污染本页面）。
  - 2. Barycenter 排序：上下双向 sweep × 24，让边交叉最小。
  - 3. 坐标分配：n 个节点用 n+1 个等分槽防止首尾贴边（每层单节点居中）。
  - 4. Edges：仅 `prerequisite.id ∈ 本页` 的边。
  - 5. 自适应 `nodeSize`：`min(52, ⌊contentW/(nMax+1)⌋ - 4)`，下限 24，保证图标不重叠。
  - 6. 用自适应 nodeSize 重算坐标，返回 `positions/edges/layers/totalContentH/nodeSize/nMax`。
- 新增 `drawTalentEdge(x1,y1,x2,y2,unlocked)`：父底中心→子顶中心的 L 形折线（`midX = (x1+x2)/2`，三段折线），子端带 3px 锚点圆。已解锁蓝、锁定灰。
- `drawMainMenuTalent` 重写：Tab→`getTalentTabRects`（未变）→命中检测区裁剪→Pass 1 边、Pass 2 节点（节点位于边之上）→滚动条。Layout 缓存于 `talentPageLayout[pageId]`（按 contentTop/usableW/nodeSizeMax/yGap 复用）。
- 结果：
  - root：nodeSize=49，4 层 (L0=core, L1=生命/攻击力, L2=6节点, L3=bombcount)，10 节点。
  - element：nodeSize=27（nMax=11），5 层，40 节点，无重叠。
  - supreme：nodeSize=33，3 层，15 节点。
  - defense：nodeSize=54，5 层，14 节点。
- 校验：四页 `minDist > nodeSize`，图标无重叠；头less 烟测不崩。提交 commit `9c15453`。
