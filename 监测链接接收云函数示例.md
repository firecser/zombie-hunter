# 监测链接接收云函数（CloudBase 版）

## 部署说明

1. 在 CloudBase 控制台创建一个 **Node.js HTTP 云函数**，命名为 `adClick`
2. 将下面的代码部署上去
3. 部署完成后获得 URL，例如：`https://xxx-service-xxx.gz.apigw.tencentcs.com/release/adClick`
4. 在 DataNexus 中将这个 URL 填入「点击监测」栏

## 云函数代码

```javascript
'use strict';

exports.main = async (event, context) => {
    // 接收腾讯广告的点击监测 POST 请求
    // event.queryStringParameters 中包含所有宏参数
    const clickData = event.queryStringParameters || {};
    
    // 记录到日志（CloudBase 日志可查）
    console.log('【广告点击】', JSON.stringify({
        click_id: clickData.__CLICK_ID__,
        click_time: clickData.__CLICK_TIME__,
        adgroup_id: clickData.__ADGROUP_ID__,
        ad_id: clickData.__AD_ID__,
        muid: clickData.__MUID__,
        ip: clickData.__IP__,
        wechat_open_id: clickData.__WECHAT_OPEN_ID__,
        timestamp: new Date().toISOString()
    }));
    
    // 返回 200 表示接收成功
    return {
        statusCode: 200,
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ ret: 0, msg: 'ok' })
    };
};
```

## 成本
CloudBase 每月有免费额度（100万次调用/月），前期足够用。
